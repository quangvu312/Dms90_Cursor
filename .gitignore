# Secrets & credentials (other env variants still ignored)
.env.*
!.env.example

# Python virtualenv
.venv/
venv/

# Node / build / portable tools
node_modules/
dist/
build/
dist-ssr/
.tools/

# Vercel local link
.vercel/

# Cache / logs / temp
cache/
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.cache/
.vite/
coverage/
tmp/
temp/
*.local

# Nested git metadata (giữ repo con local, backup chỉ source)
**/.git/
_mockup_dms90_git_backup/

# OS / editor runtime
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
