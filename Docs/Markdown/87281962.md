|  |  |
| --- | --- |
| Issue Link |  |
| Story | Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECDM-7713 |
| Epic |  |
| Feature |  |
| Description | Báo cáo cung cấp cái nhìn tổng quan về tổng số NPP trên bản đồ. Truy vấn và Giám sát tọa độ Nhà Phân Phối (NPP) trên Bản đồ, Xem các đối tượng là Nhân viên; điểm bán trực thuộc NPP trên bản đồ số |
| Document version | RedV1.0.0 |
| Document status | GreenDONE |
| Document owner | Thao.nguyen |
| Chage History | 2 |

truenone

# **Báo Cáo Truy vấn tọa độ NPP**

**Mục đích:** Báo cáo cung cấp cái nhìn tổng quan về tổng số NPP trên bản đồ. Truy vấn và Giám sát tọa độ Nhà Phân Phối (NPP) trên Bản đồ, Xem các đối tượng là Nhân viên; điểm bán trực thuộc NPP trên bản đồ số

* **Tab Độ phủ NPP:** Cho phép người dùng chọn điều kiện lọc (bắt buộc chọn Vùng/Khu vực) -> Hệ thống truy xuất tọa độ -> Hiển thị các điểm (NPP, Điểm bán) và vẽ đa giác (Polygons) bao trùm các điểm bán thuộc NPP trên bản đồ -> Người dùng tương tác (click, ẩn/hiện) để xem chi tiết.
* **Tab Chấm công:** Người dùng nhập điều kiện lọc -> Hệ thống tính toán số liệu thống kê & vẽ tọa độ NPP, tọa độ chấm công của Nhân viên lên bản đồ -> Người dùng tương tác xem chi tiết hoặc click vào thống kê để chuyển hướng xem chi tiết NPP chưa chấm công/ Chuyển hướng sang báo cáo chấm công với bộ lọc tương ứng khi xem Danh sách nhân viên đã và chưa chấm công  .

## **Tab: Độ phủ NPP**

**Màn hình**

  

**Mô tả:**

| **Tên Trường** | **Loại dữ liệu** | **Thao tác?** | **Bắt buộc?** | **Mô tả** |
| --- | --- | --- | --- | --- |
| Tìm kiếm theo | | | | |
| Vùng/ Khu vực NPP | Selectbox onechoice | Có | Có | * **Mở danh sách:**Khi người dùng nhấp vào trường "VùngVùng/ Khu vực NPP" hiển thị danh sách cây phân cấp Vùng và Khu vực   + **Phân quyền dữ liệu hiển thị theo Vùng/Khu vực của User: [HO & NPP] Phân quyền dữ liệu**   + Khi mở màn hình mặc định không chọn dữ liệu nào trong hộp chọn   + Trường hợp không chọn Khu vực → Chọn Button "Tìm kiếm" hiển thị lỗi inline: "Vùng/Khu vực NPP là bắt buộc!" * **Tìm kiếm và chọn:**     + Người dùng có thể tìm kiếm bằng cách lọc nhanh theo Mã Vùng, Tên Vùng, Mã khu vực, Tên khu vực   + Người dùng chỉ có thể chọn một Khu vực để lọc dữ liệu   + Trường hợp không có khu vực nào phù hợp với từ khóa, hệ thống hiển thị trong ô select "Trống" * **Hiển thị lựa chọn:** Khu vực đã chọn sẽ hiển thị trong hộp chọn dưới dạng nhãn (tag). * **Kết quả lọc:**    + Chỉ hiển thị dữ liệu NPP trên bản đồ thuộc Khu vực đã chọn.   + Vùng, Khu vực của NPP sẽ được định nghĩa dựa trên quận/huyện địa chỉ của NPP trên tuyến bán hàng * **Xóa lựa chọn:**   + Người dùng có thể nhấp vào biểu tượng xóa trên nhãn (tag) hoặc chọn lại trong danh sách để bỏ chọn Vùng không mong muốn.   + Trường hợp bỏ chọn Vùng trong hộp chọn thì hiển thị lỗi inline: "Vùng/Khu vực NPP là bắt buộc!" |
| Nhà phân phối | Selectbox multichoice | Có | Không | * **Mở danh sách:** Khi người dùng nhấp vào trường "Nhà phân phối" hiển thị danh sách NPP    + **Nếu chọn Khu vực → Chọn NPP:** Hiển thị các NPP thuộc Khu vực đã chọn   + **Chọn Khu vực → Chọn NPP → Đổi khu vực khác** => Tự động xóa dữ liệu NPP đã chọn.   + Khi mở màn hình mặc định không chọn dữ liệu nào trong hộp chọn   + **Trường hợp không chọn Khu vực** thì hiểu là load tất cả NPP thuộc phân quyền của người dùng đang đăng nhập: **[HO & NPP] Phân quyền dữ liệu**     - * **Người dùng phân quyền Vùng/ Khu vực → Danh sách NPP có địa chỉ thuộc Phân quyền của user**       * **Người dùng phân quyền NPP chăm sóc → Danh sách NPP được gán**       * **Người dùng phân quyền Vùng/ Khu vực và TKTT→ Danh sách các NPP trên tuyến bán hàng của nhân viên là SS; SM của TKTT đã gán.**   + Dữ liệu trong hộp chọn được sắp xếp theo ngày tạo gần nhất. * **Tìm kiếm và chọn:**    + Người dùng có thể tìm kiếm bằng cách lọc nhanh theo **Mã NPP**; **Tên NPP.** Tìm kiếm không phân biệt chữ hoa hay thường; có dấu hay không dấu   + Người dùng có thể chọn nhiều NPP để lọc dữ liệu   + Trường hợp không có NPP nào phù hợp với từ khóa, hệ thống nên hiển thị trong ô select "Trống"  * **Hiển thị lựa chọn: NPP**đã chọn sẽ hiển thị trong hộp chọn dưới dạng các nhãn (tags) "Tên NPP" * **Kết quả lọc:** Chỉ hiển thị dữ liệu NPP đã chọn/ tất cả NPP thuộc Khu vực đã chọn trên bản đồ. * **Xóa lựa chọn:**     + Người dùng có thể nhấp vào biểu tượng xóa trên các nhãn (tag) hoặc chọn lại trong danh sách để bỏ chọn NPP không mong muốn.   + Trường hợp bỏ chọn NPP trong hộp chọn thì hiểu là chọn tất cả các NPP thuộc khu vực đã chọn.   + **Chọn Khu vực → Chọn NPP → Đổi khu vực khác** => Tự động xóa tất cả dữ liệu NPP đã chọn.     - Chọn thêm khu vực  => Dữ liệu NPP đã chọn không thay đổi. |
| Điểm bán | Selectbox multichoice | Có | Không | * **Tooltip: "**Điểm bán thuộc nhà phân phối. Vui lòng chọn từ một nhà phân phối!" * **Mở danh sách:**Khi người dùng nhấp vào trường "Điểm bán"   + Khi mở màn hình mặc định không chọn dữ liệu nào trong hộp chọn.   + Nếu chọn NPP → Hiển thị các điểm bán đang hoạt động từ master data thuộc NPP đã chọn.   + Trường hợp không chọn NPP thì hiểu là load mặc định tất cả các điểm bán đang hoạt động thuộc tất cả các NPP của khu vực đã chọn * **Tìm kiếm và chọn:**    + Người dùng có thể tìm kiếm bằng cách lọc nhanh theo Mã điểm bán; Tên điểm bán; Số điện thoại điểm bán. Tìm kiếm không phân biệt chữ hoa hay thường; có dấu hay không dấu   + Người dùng có thể chọn nhiều điểm bán để lọc dữ liệu   + Trường hợp không có điểm bán nào phù hợp với từ khóa, hệ thống nên hiển thị trong ô select "Trống" * **Hiển thị lựa chọn:** Điểm bán đã chọn sẽ hiển thị trong hộp chọn dưới dạng các nhãn (tags). Mã điểm bán- Tên điểm bán. * **Kết quả lọc:** Trên bản đồ sẽ hiển thị NPP và các điểm bán đang hoạt động đã chọn/ Tất cả các điểm bán đang hoạt động thuộc NPP (nếu không chọn điểm bán nào) * **Xóa lựa chọn:**   + Người dùng có thể nhấp vào biểu tượng xóa trên các nhãn (tag) hoặc chọn lại trong danh sách để bỏ chọn   + Trường hợp bỏ chọn trong hộp chọn thì hiểu là chọn mặc định Tất cả các điểm bán đang hoạt động thuộc NPP.   + Nếu người dùng:     - Thêm NPP: Không thay đổi dữ liệu đang chọn tại ô "Điểm bán" này.     - Thay đổi NPP (bỏ NPP bất kì đã chọn và thêm NPP khác)→ Auto-Clear tất cả dữ liệu đang chọn tại ô "Điểm bán" này. |
| Button Làm mới | Button | Có | Không | **Chức năng:**   * Nút "Làm mới" cho phép người dùng tải lại dữ liệu; xóa mọi bộ lọc và kết quả tìm kiếm hiện tại, đồng thời trả về trạng thái ban đầu của màn hình. * Nút này hữu ích khi người dùng muốn bắt đầu một phiên tìm kiếm mới hoặc khi cần làm mới dữ liệu để đảm bảo rằng các dữ liệu mới nhất được hiển thị.   **Cách sử dụng:**   1. **Nhấp vào nút:** Người dùng nhấp vào nút "Làm mới" để xóa tất cả các bộ lọc, tìm kiếm, và làm mới dữ liệu trên dữ liệu. 2. **Dữ liệu làm mới:** Sau khi nhấp, lưới danh sách sẽ hiển thị toàn bộ các dữ liệu hiện có theo bộ lọc mặc định.   **Lưu ý:**   * Việc làm mới không thay đổi dữ liệu gốc mà chỉ cập nhật giao diện hiển thị của dữ liệu của màn hình * Người dùng cần lưu ý rằng tất cả các bộ lọc hoặc tìm kiếm đã thiết lập sẽ bị xóa khi nút này được nhấn. |
| Button Tìm kiếm | Button | Có | Không | **Chức năng:**   * Nút "Tìm kiếm" cho phép người dùng thực hiện tìm kiếm và áp dụng các bộ lọc đã chọn trên dữ liệu NPP; Điểm bán; Khu vực đã chọn. * Khi người dùng đã thiết lập các bộ lọc hoặc nhập từ khóa tìm kiếm, nút "Tìm kiếm" sẽ kích hoạt chức năng lọc dữ liệu theo các tiêu chí đã chọn.   **Cách sử dụng:**   1. **Thiết lập bộ lọc:** Người dùng chọn các tiêu chí tìm kiếm. 2. **Nhấp vào nút:** Sau khi thiết lập, người dùng nhấp vào nút "Tìm kiếm" để áp dụng các bộ lọc và hiển thị kết quả tìm kiếm trên bản đồ 3. **Hiển thị kết quả:** dữ liệu điểm bán sẽ cập nhật và hiển thị các hình ảnh phù hợp với các tiêu chí đã chọn.   **Lưu ý:**   * Ban đầu khi mở màn hình cũng sẽ không có load sẵn dữ liệu trên bản đồ. Bộ lọc rỗng; Bản đồ không load sẵn data. * Chọn "Tìm kiếm" →  Lọc dữ liệu NPP; Khu vực và Điểm bán  theo các tiêu chí đã chọn * Tại màn hình đã có data: Nếu không chọn tiêu chí nào khác, lưới danh sách sẽ không thay đổi khi nhấn nút "Tìm kiếm". |
| Khu vực Bản đồ (Map View) Đây là khu vực hiển thị trực quan dữ liệu sau khi bấm [Tìm kiếm] | | | | |
| Mapp mặc định |  |  |  | * **Trạng thái mặc định sau khi Tìm kiếm:**    + Bản đồ tự động zoom out/zoom in để hiển thị trọn vẹn khu vực có chứa các dữ liệu vừa tìm kiếm.   + Hiển thị chế độ **Polygons on map**:  Vẽ các đa giác nối tọa độ các điểm bán thuộc cùng 1 NPP và mỗi NPP sẽ có 1 khối Polygon với màu sắc ngẫu nhiên phân biệt. Khối Polygon này bao bọc cả NPP và các Điểm bán của NPP đó (Mặc định marker NPP  sẽ nối với các chấm đỏ là tọa độ các điểm bán thuộc NPP đó).      * + Khi click vào một khối polygon cụ thể;  các khối khác sẽ bị mờ đi và hiển thị:      * Chỉ còn lại một khối polygon vừa chọn; trong đó marker NPP  và marker điểm bán **;** nối nhau tạo thành khối polygon. |
| Hệ thống có 2 loại Marker NPP và Điểm bán; Click vào marker hiển thị popup theo đối tượng tương ứng.   * Chế độ Selected: Khi click vào một Marker bất kỳ, Marker đó phải chuyển sang trạng thái "Selected" có Icon marker to hơn, nhấp nháy, có vòng sáng viền ngoài để user biết đang xem đối tượng nào và đồng thời hiển thị popup tương ứng. * Thao tác tương tác trên bản đồ (Drag Map): Khi người dùng nhấn giữ và kéo bản đồ, Popup thông tin KHÔNG ĐƯỢC TẮT. * Vị trí Popup: Popup neo theo tọa độ của Marker đang chọn, di chuyển theo Map khi người dùng kéo bản đồ, không fix cứng tại 1 vị trí cố định trên màn hình. | | | | |
| Marker NPP | icon marker | Có | Không | Chọn 1 marker NPP   → Hiển thị popup thông tin gồm:   1. Các icon mặc định 2. Tên NPP: hiển thị tên NPP đã chọn 3. Mã NPP: hiển thị mã NPP đã chọn 4. Địa chỉ NPP: hiển thị địa chỉ NPP đã chọn 5. Tọa độ: Kinh độ: hiển thị kinh độ theo địa chỉ của NPP; Vĩ độ: hiển thị vĩ độ theo địa chỉ của NPP 6. Khu vực: hiển thị tên khu vực  theo địa chỉ NPP 7. Vùng: hiển thị tên vùng theo địa chỉ NPP 8. Icon "x" cho phép tắt popup. Trường hợp người dùng click ra ngoài bản đồ mà không click vào icon "x" vẫn tắt popup này. |
| Marker Điểm bán | icon marker | Có | Không | Khi click vào marker điểm bán trên bản đồ →  Hiển thị popup thông tin gồm:  **"Điểm bán đã gán vào tuyến"** :  Hiển thị bảng thông tin gồm:   1. Các icon mặc định 2. Tag trạng thái: [Có tuyến] - tag màu xanh 3. Tên điểm bán 4. Mã điểm bán 5. Text địa chỉ nối chuỗi của điểm bán. 6. Địa chỉ theo vị trí: Text địa chỉ theo vị trí điểm bán 7. Tọa độ: Kinh độ, Vĩ độ: hiển thị tọa độ theo vị trí điểm bán 8. Tuyến bán hàng: #Mã tuyến 1; #Mã tuyến 2; Các tuyến bán hàng thuộc NPP thỏa điều kiện phân quyền dữ liệu 9. NPP trực thuộc: Hiển thị #Mã NPP1; #Mã NPP2; Hiển thị mã NPP của điểm bán; NPP phải thuộc phân quyền người dùng mới hiển thị tại đây (Theo phân quyền dữ liệu NPP [HO & NPP] Phân quyền dữ liệu) 10. Nhân viên bán hàng: #BH0009; #BH0123, #BH0443. Nhân viên bán hàng trên các tuyến bán hàng của NPP thỏa điều kiện. 11. Icon "x" cho phép tắt popup. Trường hợp người dùng click ra ngoài bản đồ mà không click vào icon "x" vẫn tắt popup này.   **"Điểm bán chưa gán tuyến"**:   1. Các icon mặc định 2. Tên điểm bán 3. Mã điểm bán 4. Text địa chỉ nối chuỗi của điểm bán. 5. Địa chỉ theo vị trí: Text địa chỉ theo vị trí điểm bán 6. Tọa độ: Kinh độ, Vĩ độ 7. Tuyến bán hàng: Không có tuyến thì để trống. 8. NPP trực thuộc: Hiển thị #Mã NPP1; #Mã NPP2; Hiển thị mã NPP của điểm bán; NPP phải thuộc phân quyền người dùng mới hiển thị tại đây (Theo phân quyền dữ liệu NPP [HO & NPP] Phân quyền dữ liệu) 9. Nhân viên bán hàng: Không có nhân viên thì để trống. 10. Icon "x" cho phép tắt popup. Trường hợp người dùng click ra ngoài bản đồ mà không click vào icon "x" vẫn tắt popup này. |
| Chú giải bản đồ |  |  |  | * **Vị trí:** Nằm nổi (floating/anchored) trên nền bản đồ; góc dưới cùng ở giữa của khu vực Map. Nó cố định trên màn hình bản đồ dù người dùng có Pan/Drag (kéo) bản đồ đi đâu. * **Hình thức:** Dạng một thẻ (card) nền trắng, bo góc, có đổ bóng nhẹ (box-shadow) để tách biệt với nền bản đồ. * **Nội dung hiển thị:** Thể hiện định nghĩa cho 2 loại Marker của Điểm bán:    + **"Điểm bán có tuyến" :** Kèm Icon hình ngôi nhà màu cam có thêm ký hiệu hình tròn màu xanh lá cây -  chứa ký hiệu ở góc trên bên trái.   + **"Điểm bán chưa gán tuyến":** Kèm Icon hình ngôi nhà màu cam trơn (không có ký hiệu). |
| Mở rộng bản đồ/ Thu gọn bản đồ |  |  |  | Chọn icon Mở rộng ở góc phải bên trên bản đồ; khi đó bản đồ hiển thị ở dạng xem full:  Chọn icon Thu gọn  ở góc phải bên trên map khi đó bản đồ quay về dạng xem trước đó.  Dữ liệu trên map khi chuyển đổi Thu gọn/ Mở rộng không bị thay đổi.  Dấu  cho phép zoom in - Phóng to / zoom out - Thu nhỏ bản đồ khi click vào dấu cộng hoặc dấu trừ. |
| Panel "Hiển thị danh sách NPP" (Bên trái bản đồ) |  |  |  | * **Trạng thái UI mặc định:** Thu gọn vào mép trái. Chỉ hiển thị một dòng chữ "Hiển thị danh sách NPP (Xem chi tiết) * **Tương tác:**    + Checkbox ở đầu dòng chữ "Hiển thị danh sách NPP": Nằm trên cùng mặc định không chọn. Click vào checkbox để chọn =>Mở rộng Panel ra và tất cả các NPP thỏa điều kiện ở trạng thái Đã chọn. Click lại bỏ chọn -> Slide in (Đóng lại).   + Hyperlink [Xem chi tiết]: Nằm ở góc trên panel. Click vào sẽ mở ra Modal Popup "Danh sách Nhà phân phối" (Mô tả ở dưới). * **Nội dung bên trong Panel:**    + Danh sách từng NPP: Mỗi NPP là 1 cụm, bao gồm:     - Checkbox ẩn/hiện mỗi NPP (Mặc định: Checked). Khi bỏ check -> Ẩn NPP và toàn bộ điểm bán + Polygon của NPP đó trên map.     - Thông tin NPP: Icon mặc định, Tên NPP, Mã NPP, Địa chỉ NPP. |
| Modal NPP  Modal Popup "Danh sách Nhà phân phối" | | | | |
| Xem chi tiết | Hyperlink | Có | Không | Hiển thị dạng cửa sổ nổi (Modal) đè lên giao diện hiện tại khi user click vào link "Xem chi tiết" ở Panel trái.     * **Tiêu đề:** Danh sách Nhà phân phối. * **Nút Close (X):** Đóng Modal. * **Thanh tìm kiếm (Search bar):** Placeholder "Tìm kiếm theo mã, tên, số điện thoại NPP". Gõ text không phân biệt chữ hoa, chữ thường; có dấu hay không dấu + Enter/Click Icon kính lúp để tìm kiếm và hiển thị trên lưới danh sách * **Bảng dữ liệu:** Hiển thị danh sách các NPP (tương ứng với kết quả đang filter ở map). Các cột:    1. STT (Số thứ tự)   2. Tên NPP: hiển thị tên nhà phân phối   3. Mã NPP: Hiển thị mã NPP; có thể copy   4. Khu vực: Hiển thị tên khu vực của NPP   5. Vùng: Hiển thị vùng của NPP   6. Địa chỉ: hiển thị text đầy đủ địa chỉ của NPP; nối chuỗi hiển thị   7. Kinh độ: Hiển thị giá trị kinh độ của NPP theo địa chỉ   8. Vĩ độ: Hiển thị giá trị vĩ độ của NPP theo địa chỉ * Phân trang danh sách hiển thị NPP. |

## **Tab: Chấm công**

**Màn hình**

**Mô tả:**

| **Tên Trường** | **Loại dữ liệu** | **Thao tác?** | **Bắt buộc?** | **Mô tả** |
| --- | --- | --- | --- | --- |
| Tìm kiếm theo | | | | |
| Vùng/ Khu vực | Selectbox multichoice | Có | Có | * **Mở danh sách:**Khi người dùng nhấp vào trường "VùngVùng/ Khu vực" hiển thị danh sách cây phân cấp Vùng và Khu vực   + **Phân quyền dữ liệu hiển thị theo Vùng/Khu vực của User: [HO & NPP] Phân quyền dữ liệu**   + Khi mở màn hình mặc định không chọn dữ liệu nào trong hộp chọn   + Trường hợp không chọn Khu vực → Chọn Button "Tìm kiếm" hiển thị lỗi inline: "Vùng/Khu vực NPP là bắt buộc!" * **Tìm kiếm và chọn:**     + Người dùng có thể tìm kiếm bằng cách lọc nhanh theo Mã Vùng, Tên Vùng, Mã khu vực, Tên khu vực   + Người dùng có thể chọn một hoặc nhiều Khu vực/ vùng để lọc dữ liệu   + Trường hợp không có Vùng/ khu vực nào phù hợp với từ khóa, hệ thống hiển thị trong ô select "Trống" * **Hiển thị lựa chọn:** Khu vực đã chọn sẽ hiển thị trong hộp chọn dưới dạng nhãn (tag). * **Kết quả lọc:**    + Chỉ hiển thị dữ liệu NPP; các Nhân viên đã chấm công thuộc vùng/ khu vực đã chọn.   + Vùng, Khu vực của NPP sẽ được định nghĩa dựa trên quận/huyện địa chỉ của NPP trên tuyến bán hàng   + Vùng/ khu vực của nhân viên theo master data của nhân viên tại thời điểm chấm công * **Xóa lựa chọn:**   + Người dùng có thể nhấp vào biểu tượng xóa trên nhãn (tag) hoặc chọn lại trong danh sách để bỏ chọn Vùng không mong muốn.   + Trường hợp bỏ chọn Vùng trong hộp chọn thì hiển thị lỗi inline: "Vùng/Khu vực NPP là bắt buộc!" |
| Nhà phân phối | Selectbox multichoice | Có | Không | * **Mở danh sách:** Khi người dùng nhấp vào trường "Nhà phân phối" hiển thị danh sách NPP    + **Nếu chọn Khu vực → Chọn NPP:** Hiển thị các NPP thuộc Khu vực đã chọn  * + Khi mở màn hình mặc định không chọn dữ liệu nào trong hộp chọn   + **Trường hợp không chọn Khu vực** thì hiểu là load tất cả NPP thuộc phân quyền của người dùng đang đăng nhập: **[HO & NPP] Phân quyền dữ liệu**     - * **Người dùng phân quyền Vùng/ Khu vực → Danh sách NPP có địa chỉ thuộc Phân quyền của user**       * **Người dùng phân quyền NPP chăm sóc → Danh sách NPP được gán**       * **Người dùng phân quyền Vùng/ Khu vực và TKTT→ Danh sách các NPP trên tuyến bán hàng của nhân viên là SS; SM của TKTT đã gán.**   + Dữ liệu trong hộp chọn được sắp xếp theo ngày tạo gần nhất. * **Tìm kiếm và chọn:**    + Người dùng có thể tìm kiếm bằng cách lọc nhanh theo **Mã NPP**; **Tên NPP.** Tìm kiếm không phân biệt chữ hoa hay thường; có dấu hay không dấu   + Người dùng có thể chọn nhiều NPP để lọc dữ liệu   + Trường hợp không có NPP nào phù hợp với từ khóa, hệ thống nên hiển thị trong ô select "Trống"  * **Hiển thị lựa chọn: NPP**đã chọn sẽ hiển thị trong hộp chọn dưới dạng các nhãn (tags) "Tên NPP" * **Kết quả lọc:** Chỉ hiển thị dữ liệu NPP đã chọn/ tất cả NPP thuộc Khu vực đã chọn trên bản đồ. * **Xóa lựa chọn:**     + Người dùng có thể nhấp vào biểu tượng xóa trên các nhãn (tag) hoặc chọn lại trong danh sách để bỏ chọn NPP không mong muốn.   + Trường hợp bỏ chọn NPP trong hộp chọn thì hiểu là chọn tất cả các NPP thuộc khu vực đã chọn.   + **Chọn Khu vực → Chọn NPP → Đổi khu vực khác** => Tự động xóa tất cả dữ liệu NPP đã chọn.     - Trường hợp chọn thêm khu vực  => Dữ liệu NPP đã chọn không thay đổi. |
| Ngày chấm công | Datepicker | Có | Có | **Chức năng:**   * Trường này cho phép người dùng lọc danh sách dữ liệu Nhân viên đã chấm công theo ngày được chọn để hiển thị trên bản đồ. * Người dùng có thể chọn một ngày (From Date) để xem các dữ liệu trong **ngày quá khứ; ngày hiện tại**. Không cho chọn ngày tương lai.   **Cách sử dụng:**   1. **Chọn ngày:** Người dùng chọn một ngày bằng cách nhấp vào biểu tượng lịch hoặc nhập trực tiếp ngày vào trường dữ liệu. 2. **Hiển thị kết quả:** Danh sách dữ liệu Nhân viên chấm công theo ngày được chọn sẽ được lọc theo. 3. **Điều chỉnh:** Người dùng có thể thay đổi ngày   **Lưu ý:**   * Trường "Ngày chấm công" sẽ disable những ngày > todaty() * Khi mở màn hình mặc định Ngày hiện tại. * Khi xóa không chọn ngày nào => Chọn Button "Tìm kiếm" tự động điền ngày hiện tại mặc định. |
| Kênh nhân viên | Selectbox multichoice | Có | Không | * **Mở danh sách:**Khi người dùng nhấp vào trường "Kênh nhân viên"   + Khi nhấn vào sẽ load hết dữ liệu danh sách Kênh bán hàng đang còn ở trạng thái hoạt động, dữ liệu được lấy từ màn hình Kênh bán hàng, danh sách hiển thị Tên kênh bán hàng.   + Cho phép chọn nhiều.   + Mặc định trống. * **Tìm kiếm và chọn:**    + Người dùng có thể tìm kiếm bằng cách lọc nhanh theo Mã Kênh bán hàng, Tên Kênh bán hàng   + Người dùng có thể chọn nhiều Kênh bán hàng để lọc dữ liệu   + Trường hợp không có Kênh bán hàng nào phù hợp với từ khóa, hệ thống nên hiển thị trong ô select "Trống" * **Hiển thị lựa chọn:** Kênh bán hàng đã chọn sẽ hiển thị trong hộp chọn dưới dạng các nhãn (tags). * **Kết quả lọc:** Hiển thị dữ liệu   + Nhân viên bán hàng thuộc Kênh bán hàng đã chọn.   + Nhân viên không thuộc kênh nào tức là lúc nào cũng hiển thị trên lưới danh sách * **Xóa lựa chọn:**   + Người dùng có thể nhấp vào biểu tượng xóa trên các nhãn (tag) hoặc chọn lại trong danh sách để bỏ chọn Kênh bán hàng không mong muốn.   + Trường hợp bỏ chọn Kênh bán hàng trong hộp chọn thì hiểu là chọn tất cả các Kênh bán hàng trên hệ thống |
| Nhân viên | Selectbox multichoice | Có | Không | * **Mở danh sách**: Khi người dùng nhấp vào trường "Nhân viên" hiển thị danh sách Nhân viên Active và inactive"   + Thuộc phân quyền của người dùng đang đăng nhập **[HO & NPP] Phân quyền dữ liệu**:       - Chức vụ của nhân viên = Nhân viên bán hàng/Giám sát bán hàng/ Quản lý khu vực/ Quản lý vùng/ Giám đốc toàn quốc     - **Người dùng phân quyền Vùng/ Khu vực → Danh sách nhân viên thuộc Vùng; Khu vực người dùng login**     - **Người dùng phân quyền NPP chăm sóc → Danh sách nhân viên trên tuyến bán hàng của NPP chăm sóc**     - **Người dùng gán TKTT → Danh sách nhân viên thuộc cây saleforce TKTT đã gán và chính nó**   + Nếu chọn NPP → Hiển thị các NVBH thỏa phân quyền thuộc NPP đã chọn   + Chọn **Kênh nhân viên** => hiển thị danh sách NVBH thuộc kênh bán hàng đã chọn   + Khi mở màn hình mặc định không chọn dữ liệu nào trong hộp chọn. Hiểu là chọn tất cả các NVBH thỏa phân quyền của Vùng/Khu vực đã chọn. * **Tìm kiếm và chọn:**    + Người dùng có thể tìm kiếm bằng cách lọc nhanh theo Mã Nhân viên, Tên Nhân viên, Số điện thoại nhân viên. tìm kiếm không phân biệt chữ hoa thường; có hay không dấu   + Người dùng có thể chọn nhiều Nhân viên để lọc dữ liệu   + Trường hợp không có Nhân viên nào phù hợp với từ khóa, hệ thống nên hiển thị trong ô select "Trống" * **Hiển thị lựa chọn:** Nhân viên đã chọn sẽ hiển thị trong hộp chọn dưới dạng các nhãn (tags). * **Kết quả lọc:** Chỉ hiển thị dữ liệu của nhân viên đã chọn. * **Xóa lựa chọn:**    + Người dùng có thể nhấp vào biểu tượng xóa trên các nhãn (tag) hoặc chọn lại trong danh sách để bỏ chọn Nhân viên không mong muốn.   + Trường hợp bỏ chọn Nhân viên trong hộp chọn thì hiểu là chọn tất cả các Nhân viên trên hệ thống (không phụ thuộc điều kiện của hộp chọn này) |
| Button Làm mới | Button | Có | Không | **Chức năng:**   * Nút "Làm mới" cho phép người dùng tải lại dữ liệu; xóa mọi bộ lọc và kết quả tìm kiếm hiện tại, đồng thời trả về trạng thái ban đầu của màn hình. * Nút này hữu ích khi người dùng muốn bắt đầu một phiên tìm kiếm mới hoặc khi cần làm mới dữ liệu để đảm bảo rằng các dữ liệu mới nhất được hiển thị.   **Cách sử dụng:**   1. **Nhấp vào nút:** Người dùng nhấp vào nút "Làm mới" để xóa tất cả các bộ lọc, tìm kiếm, và làm mới dữ liệu trên dữ liệu. 2. **Dữ liệu làm mới:** Sau khi nhấp, lưới danh sách sẽ hiển thị toàn bộ các dữ liệu hiện có theo bộ lọc mặc định.   **Lưu ý:**   * Việc làm mới không thay đổi dữ liệu gốc mà chỉ cập nhật giao diện hiển thị của dữ liệu của màn hình * Người dùng cần lưu ý rằng tất cả các bộ lọc hoặc tìm kiếm đã thiết lập sẽ bị xóa khi nút này được nhấn. |
| Button Tìm kiếm | Button | Có | Không | **Chức năng:**   * Nút "Tìm kiếm" cho phép người dùng thực hiện tìm kiếm và áp dụng các bộ lọc đã chọn trên dữ liệu NPP; Nhân viên * Khi người dùng đã thiết lập các bộ lọc hoặc nhập từ khóa tìm kiếm, nút "Tìm kiếm" sẽ kích hoạt chức năng lọc dữ liệu NPP; Nhân viên theo các tiêu chí đã chọn.   **Cách sử dụng:**   1. **Thiết lập bộ lọc:** Người dùng chọn các tiêu chí tìm kiếm. 2. **Nhấp vào nút:** Sau khi thiết lập, người dùng nhấp vào nút "Tìm kiếm" để áp dụng các bộ lọc và hiển thị kết quả tìm kiếm trong thống kê; Lưới danh sách; trên bản đồ 3. **Hiển thị kết quả:** dữ liệu điểm bán sẽ cập nhật và hiển thị các hình ảnh phù hợp với các tiêu chí đã chọn.   **Lưu ý:**   * Ban đầu khi mở màn hình cũng sẽ không có load sẵn dữ liệu trên bản đồ. * Chọn Vùng/ khu vực bắt buộc và các tiêu chí khác (nêú có) → Chọn  "Tìm kiếm" →  Lọc dữ liệu NPP; Nhân viên theo các tiêu chí đã chọn/ mặc định thỏa phân quyền người dùng. * Tại màn hình đã có data: Nếu không chọn tiêu chí nào khác, lưới danh sách sẽ không thay đổi khi nhấn nút "Tìm kiếm". |
| Khu vực Thống kê tổng quan | | | | |
| Nhân viên/ NPP | Datacolumn | Có | Không | Hiển thị tỷ lệ: [Tổng số NV tìm thấy] / [Tổng số NPP tìm thấy]  Click vào cụm thống kê màu xanh lá nhạt hoặc dấu '>' → Mở Modal Popup "Danh sách Nhà phân phối" (Giao diện và chức năng Modal này dùng chung/giống hệt với Tab "Độ phủ NPP" đã phân tích tại Modal Popup "Danh sách Nhà phân phối"). |
| NV đã chấm công | Datacolumn | Có | Không | Hiển thị [Tổng số NV đã có dữ liệu chấm công] trong ngày tìm kiếm  Click vào cụm thống kê màu xanh nước biển nhạt hoặc dấu '>' → Redirect đến màn hình Báo cáo chấm công   * Khi redirect phải mang theo (pass params) các điều kiện lọc hiện tại của Map sang màn hình Báo cáo chấm công và tự động áp dụng bộ lọc đó (Trạng thái: Đã chấm công). * Link doc: [HO][HT] Báo cáo Chấm công |
| NV chưa chấm công | Datacolumn | Có | Không | Hiển thị [Tổng số NV chưa có dữ liệu chấm công] trong ngày tìm kiếm  Nhân viên này được tính bởi công thức **Giá trị hiển thị =  [Tổng số nhân viên thỏa phân quyền USer login] - [NV đã chấm công]**  Click vào cụm thống kê màu vàng nhạt hoặc dấu '>' → Redirect đến màn hình Báo cáo chấm công.   * Khi redirect phải mang theo (pass params) các điều kiện lọc hiện tại của Map sang màn hình Báo cáo chấm công và tự động áp dụng bộ lọc đó (Trạng thái: Chưa chấm công). * Link doc: [HO][HT] Báo cáo Chấm công |
| Khu vực Bản đồ (Map View)   * Hiển thị vị trí tọa độ của NPP và vị trí thực tế lúc Nhân viên thực hiện chấm công.  * Cho phép "Mở rộng bản đồ/ Thu gọn bản đồ"; "Zoom in / Zoom out" bản đồ | | | | |
| Chú giải |  |  |  | Ở góc dưới cùng bản đồ luôn hiển thị chú giải phân biệt Icon: Nhân viên đã chấm công, Nhà phân phối.      Hệ thống có 2 loại marker hiển thị sau khi chọn button "Tìm kiếm": Marker Nhân viên bán hàng và Marker Nhà phân phối  Khi User click vào một Marker (NPP hoặc NV) bất kỳ:   * Marker đó phải phóng to hơn (Scale up). * Có hiệu ứng nhấp nháy (Blinking/Pulsing). * Có vòng sáng viền ngoài (Glowing ring/Box-shadow) bao quanh để làm nổi bật đối tượng đang được focus. * Hiển thị  / và đồng thời mở popup (Xem mô tả bên dưới)   Hành vi thao tác trên bản đồ: Khi User nhấn giữ chuột và kéo bản đồ đi nơi khác:   * Popup thông tin của Marker đang mở KHÔNG ĐƯỢC TẮT/ĐÓNG LẠI. * Popup phải được neo (anchor) dính chặt vào tọa độ của Marker đó. Khi bản đồ di chuyển, Popup di chuyển theo mượt mà trên màn hình, không bị fix cứng tại một vị trí tọa độ màn hình. |
| Marker NPP | Icon | Có | Không | Hiển thị popup thông tin gồm:   1. Các icon mặc định 2. Tên NPP: hiển thị tên NPP đã chọn 3. Mã NPP: hiển thị mã NPP đã chọn 4. Địa chỉ NPP: hiển thị địa chỉ NPP đã chọn 5. Tọa độ: Kinh độ: hiển thị kinh độ theo địa chỉ của NPP; Vĩ độ: hiển thị vĩ độ theo địa chỉ của NPP 6. Khu vực: hiển thị tên khu vực  theo địa chỉ NPP 7. Vùng: hiển thị tên vùng theo địa chỉ NPP 8. Icon "x" cho phép tắt popup. Trường hợp người dùng click ra ngoài bản đồ mà không click vào icon "x" vẫn tắt popup này. |
| Marker Nhân viên | Icon | Có | Không | Hiển thị Modal/Popup chi tiết (như hình 2) gồm:   1. Header: Avatar, Tên nhân viên, (Mã nhân viên) - Chức vụ (VD: Nhân viên bán hàng). 2. Quản lý trực tiếp: Hiển thị Mã - Tên quản lý. 3. Chấm công: Thời gian Từ - đến (Chưa có thì để trống) 4. Tại: Địa chỉ text nơi chấm công. 5. Tọa độ: Kinh độ, Vĩ độ. 6. Khoảng cách: Khoảng cách lệch so với điểm chuẩn (VD: 50m). 7. Lý do vượt khoảng cách: Text lý do (Nếu không có lý do thì ẩn cụm này) 8. NPP trực thuộc: Hiển thị #MãNPP Hiển thị mã NPP của nhân viên. NPP phải thuộc phân quyền người dùng mới hiển thị tại đây (Theo phân quyền dữ liệu NPP [HO & NPP] Phân quyền dữ liệu) 9. Tuyến bán hàng: Hiển thị #MãTuyến. Hiển thị các tuyến bán hàng của nhân viên thuộc NPP theo phân quyền 10. Icon "x" cho phép tắt popup. Trường hợp người dùng click ra ngoài bản đồ mà không click vào icon "x" vẫn tắt popup này. |