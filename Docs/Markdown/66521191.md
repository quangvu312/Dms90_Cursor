|  |  |
| --- | --- |
| Issue Link |  |
| Story |  |
| Epic |  |
| Feature |  |
| Description | SD/ASM/RSM/SS đăng nhập và duyệt đăng ký tích lũy.  Duyệt 1 cập realtime |
| Document version | RedV1.0.0  23/5/25:   * [Gắn rule chung load page, refresh và loadding data](https://kb.finviet.com.vn/display/DMSNEW/Manager+App) * Bỏ mã màu đã mô tả trong doc, Về màu sắc các trạng thái có trong UI =>[gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0) * Back màn hình Danh sách đăng ký sau khi chọn nhân viên => trở về màn hình Xét duyệt Đăng ký CTTL * Back màn hình Chi tiết đăng ký => về màn hình Danh sách đăng ký → focus vào tab tương ứng với trạng thái trên màn hình * Neo search bar nhân viên và Các tab trạng thái trên màn hình Danh sách đăng ký   12/06/2025: Không hiển thị với CTTL có loại chương trình = merchant   RedV1.0.2 Chọn ngày xem CTTL → Load danh sách CTTL có thời gian chương trình giao trong đoạn thời gian đã chọn  RedV1.1.0 : Chọn tìm nhân viên hiển thị màn hình Danh sách nhân viên, bỏ icon cuốn sổ, bỏ màn hình Tìm kiếm nhân viên.  RedV1.2.0 : Define rule hiển thị phiếu đăng ký theo từng role login và chọn đổi nhân viên cấp dưới để xem dữ liệu  YellowV1.2.1 : Cập nhật rule hiển thị Chương trình trên APP QL |
| Document status | GreenDONE |
| Document owner |  |
| Chage History | 2 |

truenone

## Xét duyệt đăng ký CTTL

* Chức năng này yêu cầu phải thực hiện chấm công đầu ngày theo quy tắc [Rule cho các chức năng bắt buộc chấm công đầu ngày](https://kb.finviet.com.vn/display/DMSNEW/Manager+App)

* Trường hợp người dùng đã chấm công đầu ngày: Hệ thống mở màn hình như sau:

| Trường dữ liệu | Mô tả |
| --- | --- |
| Hiển thị mặc định | * Hệ thống hiển thị mặc định danh sách các CTTL:   + Ở trạng thái "Đang diễn ra": Hiển thị theo độ ưu tiên, CTTL nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình tích lũy gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Hiển thị tất cả các CTTL áp dụng cho các điểm bán thuộc Vùng - khu vực của tài khoản đăng nhập, dựa  vào master điểm bán của các đối tượng áp dụng để xác định Vùng - Khu vực      * + - ~~CTTL không chọn đối tượng nào => hiểu là all vùng-Khu vực~~     - ~~CTTL chọn Vùng -Khu vực => tức là Vùng-Khu vực CTTL phải thuộc vùng-Khu vực user login mới hiển thị~~     - ~~CTTL chọn NPP -> Vùng khu vực NPP phải thuộc Vùng -Khu vực user login~~     - ~~CTTL chọn Điểm bán → Dựa vào master điểm bán mapping Vùng-Khu vực thuộc vùng-Khu vực user login để hiển thị CTTL~~     - ~~CTTL chọn Hạng/Vị trí/ Loại điểm bán/ Kênh bán hàng => Lấy tất cả các điểm bán thuộc Hạng/Vị trí/ Loại điểm bán/ kênh bán hàng đã chọn. Xét Vùng -khu vực của các điểm bán để hiển thị CTTL theo user login app quản lý~~      * + - Không hiển thị CTTL có Loại chương trình = Merchant   YellowV1.2.1 : Cập nhật rule hiển thị Chương trình trên APP QL    * + - * Một Chương trình tích lũy (CTTL) sẽ hiển thị trên danh sách của Quản lý nếu thỏa mãn điều kiện sau:          + **Điều kiện:** Cấu hình đối tượng áp dụng của CTTL – Dựa vào Danh sách điểm bán trong các tuyến thuộc quyền quản lý của User.           - Danh sách điểm bán trong các tuyến thuộc quyền quản lý của User theo Đối tượng áp dụng của CTTL:             * Không chọn đối tượng: Tất cả các điểm bán             * Chọn Vùng/ Khu vực: Tất cả các điểm bán trong các tuyến thuộc quyền quản lý của User login và Các tuyến này thuộc vùng/ khu vực của chương trình             * Chọn NPP: Tất cả các điểm bán trong các tuyến thuộc quyền quản lý của User login và các tuyến này của NPP thuộc chương trình             * Chọn Điểm bán: điểm bán trong các tuyến thuộc quyền quản lý của User login và các điểm bán này thuộc chương trình             * **Chọn Thuộc tính** (Hạng/Vị trí/Kênh...): điểm bán trong các tuyến thuộc quyền quản lý của User login thỏa điều kiện thuộc tính của chương trình             * Chọn nhiều: Nếu một chương trình cài đặt nhiều điều kiện: Xét chỉ cần một điểm bán trên tuyến thỏa điều kiện theo Logic AND thì hiển thị   + Thời gian từ đầu tháng đến cuối tháng của ngày hiện tại (Ví dụ ngày hiện tại là 10/12/2024, thì lấy thời gian từ 1/12 - 31/12)   + Thời gian lọc danh sách CTTL lấy theo YellowV1.2.1 thời gian chương trình giao với thời gian bộ lọc sẽ hiển thị danh sách chương trình trên lưới danh sách   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item |
| Tìm kiếm | placeholder: Tìm theo Tên, Mã chương trình   * Khi người dùng nhập dữ liệu, hệ thống sẽ lọc danh sách các chương trình đang hiển thị bên dưới theo từ khóa đã nhập. * Có thể hoạt động: nhấn nút search trên keyboard để search * 3 tab chung bộ lọc: Đang diễn ra, Ngưng hoạt động, Kết thúc |
| Bộ lọc ngày | * Thời gian từ đầu tháng đến cuối tháng của ngày hiện tại (Ví dụ ngày hiện tại là 10/12/2024, thì lấy thời gian từ 1/12 - 31/12) * Thời gian lọc danh sách CTTL   + RedV1.0.2 Chọn ngày xem CTTL → Load danh sách CTTL có thời gian chương trình giao trong đoạn thời gian đã chọn * User có thể nhấn để chọn lại thời gian      * Đến ngày >= Từ ngày * Từ ngày → Đến ngày <= 90 ngày * Không chọn ngày nào → Xác nhận → Hiển thị ngày hôm nay |
| Tabs lọc trạng thái | Các tab: "Đang diễn ra", "Ngưng hoạt động", "Kết thúc".  Hiển thị theo độ ưu tiên, CTTL nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình tích lũy gần hiện tại nhất trên cùng- trùng thì hiển thị random  **Tab Đang diễn ra:**   * Hệ thống hiển thị mặc định danh sách các CTTL:   + Ở trạng thái "Đang diễn ra"   + Có phân quyền dữ liệu của nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Hiển thị theo độ ưu tiên, CTTL nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình tích lũy gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Thời gian lọc danh sách CTTL lấy theo ngày bắt đầu chương trình theo thời gian đã lọc bên trên   + Hiển thị tag X Phiếu chờ duyệt (đếm tất cả các phiếu chờ duyệt của CTTL)   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item     **Tab Ngưng hoạt động**   * Hệ thống hiển thị mặc định danh sách các CTTL:   + Ở trạng thái "Ngưng hoạt động"   + Có phân quyền dữ liệu của nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Hiển thị theo độ ưu tiên, CTTL nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình tích lũy gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Thời gian lọc danh sách CTTL lấy theo ngày bắt đầu chương trình theo thời gian đã lọc bên trên   + Thẻ tag - X phiếu đăng ký: Đếm tất cả các  phiếu đã đăng ký có trạng thái (Đã duyệt; Từ chối duyệt; Hết hạn duyệt; Ngưng hoạt động)   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item   **Tab Kết thúc**   * Hệ thống hiển thị mặc định danh sách các CTTL:   + Ở trạng thái "Kết thúc"   + Có phân quyền dữ liệu của nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Hiển thị theo độ ưu tiên, CTTL nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình tích lũy gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Thời gian lọc danh sách CTTL lấy theo ngày bắt đầu chương trình theo thời gian đã lọc bên trên   + thẻ tag - X phiếu đăng ký: Đếm tất cả các  phiếu đã đăng ký có trạng thái (Đã duyệt; Từ chối duyệt; Hết hạn duyệt; Ngưng hoạt động)   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item |
|  | Icon date: Ngày bắt đầu chương trình → Ngày kết thúc chương trình  Tên chương trình: hiển thị 2 dòng, nhiều hơn hiển thị dấu ba chấm ....  text hyperlink: Xem chi tiết → để xem Danh sách đăng ký chương trình |
|  | Icon date: Ngày bắt đầu chương trình → Ngày kết thúc chương trình  Tên chương trình: hiển thị 2 dòng, nhiều hơn hiển thị dấu ba chấm ....  text hyperlink: Xem chi tiết → để xem Danh sách đăng ký chương trình |

Danh sách đk

## Danh sách đăng ký

| Trường dữ liệu | Mô tả |
| --- | --- |
| Hiển thị mặc định | * Hệ thống hiển thị mặc định danh sách đăng ký của chương trình tích lũy đã chọn. DS điểm bán đăng ký thuộc phân quyền tất cả các nhân viên của user đăng nhập.   + Mặc định hiển thị tab Chờ duyệt   + Cho phép scroll vuốt sang phải để xem nhiều hơn; trái để xem tab trước đó   + RedV1.2.0 Có nhân viên chăm sóc trên tuyến bán hàng của điểm bán là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập (không nhìn thấy dữ liệu của bản thân người đăng nhập nếu là SS)   + Hiển thị Đã hết thông tin ở cuối page   + Có phân trang hiển thị |
| Tìm kiếm | RedV1.1.0 : Chọn tìm nhân viên hiển thị màn hình Danh sách nhân viên, bỏ icon cuốn sổ, bỏ màn hình Tìm kiếm nhân viên     * Chức năng được mô tả ở: [[Manager App] Chọn nhân viên và xem danh sách cây Salesforce nhân viên](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53035242) * Trường hợp người dùng chọn một nhân viên từ màn hình tìm kiếm, màn hình sẽ thay đổi như sau      * Danh sách yêu cầu duyệt đăng ký của điểm bán theo thời gian sẽ hiển thị theo nhân viên được chọn * Thanh search sẽ áp dụng cho tất cả các màn hình * **Tất cả nhân viên:**Quay về màn hình mặc định tất cả nhân viên có thanh search như ban đầu * **Đổi:** Chuyển tới màn hình Tìm kiếm nhân viên như mô tả bên trên để user chọn nhân viên khác. * **Chọn back** về màn hình:   + 23/5/25:      - Back màn hình Danh sách đăng ký sau khi chọn nhân viên => trở về màn hình Xét duyệt Đăng ký CTTL     - Neo search bar nhân viên và Các tab trạng thái trên màn hình Danh sách đăng ký   RedV1.2.0 : Define rule hiển thị phiếu đăng ký theo từng role login và chọn đổi nhân viên cấp dưới để xem dữ liệu  Phiếu đăng ký hiển thị dựa vào: Điểm bán trên tuyến của nhân viên chăm sóc nào thì hiển thị theo nhân viên chăm sóc trên tuyến đó.  **Trường hợp A: User Login là SD (Giám đốc vùng)** Vị trí cao nhất, quản lý toàn bộ RSM > ASM > SS > SM.   | Hành động chọn (Filter) | Dữ liệu hiển thị trên màn hình "Danh sách đăng ký" | | --- | --- | | **Mặc định (Tất cả)** | Toàn bộ phiếu đăng ký của **tất cả cấp dưới** (RSM, ASM, SS, SM) thuộc SD quản lý. | | **Chọn RSM** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **cấp dưới của RSM đó** phụ trách (ASM, SS, SM thuộc nhánh RSM này). | | **Chọn ASM** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **cấp dưới của ASM đó** phụ trách (SS, SM thuộc nhánh ASM này). | | **Chọn SS** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **SS và SM thuộc quyền quản lý của SS đó** phụ trách. | | **Chọn SM** | Chỉ hiển thị các phiếu đăng ký tại điểm bán do **chính SM đó** phụ trách tuyến. |   **Trường hợp B: User Login là RSM hoặc ASM**   | Hành động chọn (Filter) | Dữ liệu hiển thị trên màn hình "Danh sách đăng ký" | | --- | --- | | **Mặc định (Tất cả)** | Toàn bộ phiếu đăng ký của **tất cả cấp dưới** (SS, SM) thuộc RSM/ ASM quản lý. | | **Chọn Cấp quản lý dưới ASM** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **cấp dưới của ASM đó** phụ trách (SS, SM thuộc nhánh ASM này). | | **Chọn SS** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **SS và SM thuộc quyền quản lý của SS đó** phụ trách. | | **Chọn SM** | Chỉ hiển thị các phiếu đăng ký tại điểm bán do **chính SM đó** phụ trách tuyến. |   **Trường hợp C: User Login là SS (Giám sát bán hàng)**   | Hành động chọn (Filter) | Dữ liệu hiển thị trên màn hình "Danh sách đăng ký" | Giải thích Logic | | --- | --- | --- | | **Mặc định (Tất cả)** | Toàn bộ phiếu đăng ký của **tất cả các SM** thuộc đội nhóm do SS này quản lý. | **Lưu ý:** Không hiển thị phiếu đăng ký của các điểm bán do chính SS phụ trách | | **Chọn SM** | Chỉ hiển thị các phiếu đăng ký tại điểm bán do **chính SM đó** phụ trách tuyến. | theo tuyến bán hàng của nhân viên phụ trách |   Mô tả thêm:   1. **Back:** Khi đang xem dữ liệu của một nhân viên được chọn (Ví dụ: SD đang xem SM Nguyễn Văn A), nếu nhấn nút **Back** hoặc nút **"Tất cả nhân viên"**, màn hình phải quay về trạng thái Mặc định (View tổng của SD) và reset lại thanh search. 2. **Trạng thái Tab:** Việc chọn nhân viên không làm thay đổi Tab đang đứng     1. Ví dụ: Đang đứng Tab "Chờ duyệt" và chọn nhân viên A -> Hiển thị danh sách "Chờ duyệt" của nhân viên A).    2. Các Tab khác hiển thị dữ liệu theo nhân viên được chọn |
|  | 23/5/25: Bỏ mã màu đã mô tả trong doc, Về màu sắc các trạng thái có trong UI =>[gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0)   Icon date: Ngày bắt đầu chương trình → Ngày kết thúc chương trình  Tên chương trình: hiển thị 2 dòng, nhiều hơn hiển thị dấu ba chấm ....  Click item hoặc dấu > để xem chi tiết chương trình |
| Tab trạng thái đăng ký | Scroll ngang để xem các tab trạng thái  Hiển thị danh sách theo bộ lọc của nhân viên (Search bar đầu màn hình)  1/ Khi chọn nhân viên ở search bar đầu màn hình => Hiển thị tất cả các điểm bán đăng ký theo các trạng thái tương ứng của CTTL 3/ Khi chọn search bar Tên, mã điểm bán sẽ lọc lại các điểm bán có trong danh sách sẵn có. 4/ Khi đổi nhân viên khác để xem => reload lại từ đầu, load danh sách theo phân quyền nhân viên đã chọn. Khi đó text bên trong thanh search Tên, mã điểm bán sẽ bị xóa hết đi  23/5/25:   * Bỏ mã màu đã mô tả trong doc, Về màu sắc các trạng thái có trong UI =>[gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0) * Neo search bar nhân viên và Các tab trạng thái trên màn hình Danh sách đăng ký   **Tab Chờ Duyệt**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Chờ duyệt"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Đã duyệt**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Đã duyệt"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Từ Chối**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Từ chối"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Hết Hạn**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Hết hạn"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Ngưng hoạt động**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán có trạng thái:   + Ở trạng thái "Ngưng hoạt động"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền) |
| Tìm kiếm điểm bán | placeholder: Tìm theo Tên, Mã điểm bán   * Khi nhập dữ liệu trong search bar Tên, mã điểm bán sẽ lọc lại các điểm bán có trong danh sách sẵn có bên dưới    + Khi đổi nhân viên khác để xem => reload lại từ đầu, load danh sách theo phân quyền nhân viên đã chọn. Khi đó text bên trong thanh search Tên, mã điểm bán sẽ bị xóa hết đi * Có thể hoạt động: nhấn nút search trên keyboard để search * Input search: tên, mã điểm bán (Riêng cho từng tab) * Khi search điểm bán hiển thị danh sách thỏa điều kiện search -> Chọn xóa text search thì load lại page * **Nút “Chọn tất cả" / "Bỏ chọn tất cả” sẽ ẩn đi** nếu người dùng đang ở trạng thái lọc, tức là khi chọn search điểm bán thì ẩn text "Chọn tất cả" / "Bỏ chọn tất cả" |
| Chọn tất cả | * Button chọn tất cả , bỏ chọn tất cả, chỉ có trong tab Chờ Duyệt (chọn tất cả item đang hiển thị) * Khi chọn tất cả sẽ hiển thị UI như sau:      * Trường hợp có >=2 điểm bán được chọn, UI sẽ hiển thị thêm 2 button Từ chối, Duyệt Tất cả   + Duyệt tất cả → Hiển thị popup thông báo: Bạn chắc chắn muốn duyệt tất cả các phiếu đã chọn?     - Chắc chắn:       * Tất cả điểm bán chuyển sang trạng thái Đã duyệt       * Lưu thông tin tài khoản người dùng thực hiện đồng ý duyệt đăng ký của điểm bán.     - Xem lại: Đóng popup và quay về màn hình hiện tại, vẫn giữ các checkbox đã check.   + Từ chối:     - Hiển thị popup để chọn Lý do từ chối duyệt đăng ký của điểm bán như sau:      * + - Danh sách lý do lấy từ [Danh sách dữ liệu chung](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53023360) loại = Lý do từ chối duyệt CTTL và thêm Lý do Khác, trường hợp user chọn Lý do khác phải nhập thông tin Lý do (free text, 100)       * Không nhập lý do từ chối/ Không chọn option nào => Disable button "Xác nhận"     - Nhấn xác nhận →       * Tất cả điểm bán chuyển trạng thái Từ chối duyệt.       * Lưu thông tin lý do từ chối duyệt đăng ký của điểm bán       * Lưu thông tin tài khoản người dùng thực hiện từ chối duyệt đăng ký của điểm bán.     - User chọn nhiều điểm bán và nhấn từ chối thì Lý do được chọn sẽ áp dụng cho tất cả điểm bán được chọn từ chối duyệt. * Ghi nhận Từ chối/Duyệt thành công, hiển thị popup, auto đóng sau 2s:      * Ghi nhận Từ chối/Duyệt thất bại và không có thông tin điểm bán cụ thể bị thất bại, hiển thị popup kèm lỗi như sau:      * Khi chọn Duyệt/Từ chối những case thỏa điều kiện thì pass; Case có lỗi  (có một điểm bán xác định bị lỗi) hiển thị 1 popup lỗi - liệt kê tất cả các lỗi:  **Đã xảy ra lỗi**   + **Chương trình tích lũy @ tên chương trình đã bị ngưng hoạt động, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Đã duyệt, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Từ chối duyệt, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Ngưng hoạt động, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Hết hạn duyệt, vui lòng thử lại!**   + **"Điểm bán bị ngưng hoạt động":** Mã DB - Tên DB đã bị ngưng hoạt động. Vui lòng thử lại!   + **"Tuyến bán hàng bị ngưng hoạt động":** Mã DB - Tên DB có tuyến bán hàng ngưng hoạt động. Vui lòng thử lại!   + **Trường hợp này lỗi** khác:     - **@Mã DB - tên điểm bán đã xảy ra lỗi, vui lòng thử lại!**   Tắt popup:    * + - Những điểm bán duyệt/ từ chối thành công sẽ tự động chuyển qua các tab trạng thái tương ứng     - Duyệt/ từ chối thất bại: hiển thị highlight các lỗi inline - Tất cả các action như search/chọn tab khác/ Pull to refresh thì clear lỗi inline. Nội dung lỗi cho từng điểm bán cụ thể **"Tuyến bán hàng đã bị ngưng hoạt động!"; "Điểm bán đã bị ngưng hoạt động!";** lỗi khác chưa define ở trên báo **"Đã xảy ra lỗi!"**      * Sau khi nhấn Chọn Tất cả → button sẽ đổi tên thành Bỏ chọn tất cả * Nhấn Bỏ chọn tất cả→ button đổi tên thành Chọn tất cả và uncheck toàn bộ trên lưới danh sách    Mô tả thêm về Text Chọn tất cả | Bỏ chọn tất cả:   * + Mặc định hiển thị: "Chọn tất cả"   + Chỉ hiển thị text "Bỏ chọn tất cả" khi tick chọn tất cả các item đang load trên màn hình     - Ví dụ:       * Load page 1: Check 1 -> 9 trên 10 item: "Chọn tất cả"       * Check 10/10: "Bỏ chọn tất cả"       * Load thêm page 2 -> check 10/20: "Chọn tất cả"   + Khi thay đổi text trong ô tìm theo điểm bán -> ẩn text "Chọn tất cả" | "Bỏ chọn tất cả"   + Khi thay đổi nhân viên -> refresh page -> hiển thị như mặc định |
| Item Điểm bán đăng ký    **Tab Chờ duyệt** | Default hiển thị uncheck  Hiển thị dữ liệu có phân trang  Sau khi check chọn hiển thị UI như hình:    -- Thông tin item điểm bán đăng ký   * Tạo ngày + Ngày tạo đăng ký CTTL cho điểm bán * Bởi: Tên nhân viên - Chức vụ nhân viên đăng ký CTTL cho điểm bán * Hình ảnh:   + Hình ảnh điểm bán   + Trường hợp có nhiều ảnh sẽ hiển thị ảnh chụp gần nhất   + Trường hợp không có hình ảnh sẽ hiển thị hình mặc định: * Tên điểm bán: Thông tin tên điểm bán (nhiều hơn 2 dòng hiển thị dấu ba chấm) * Địa chỉ: Thông tin địa chỉ của điểm bán, (Địa chỉ điểm bán: hiển thị dấu ... nếu nhiều hơn 2 dòng) * Mốc tích lũy n - Số suất x: Nối chuỗi hiển thị Mốc tích lũy và số suất đăng ký của điểm bán * Hình ảnh: hiển thị 3 hình, nhiều hơn thì hiển thị 2 hình và ô +(n-2), icon * Chọn item hoặc dấu > hiển thị màn hình Thông tin đăng ký   ---   * Trạng thái: hiển thị trạng thái đăng ký của điểm bán, chỉ hiển thị button  với đăng ký có trạng thái Chờ duyệt * Duyệt → Hiển thị popup thông báo: Bạn chắc chắn muốn duyệt Phiếu đăng ký đã chọn?  * + Chắc chắn:     - Đăng ký của Điểm bán chuyển sang trạng thái đk là Đã duyệt     - Lưu thông tin tài khoản người dùng thực hiện đồng ý duyệt đk điểm bán.  * + Xem lại: Đóng popup và quay về màn hình hiện tại, vẫn giữ các checkbox đã check. * Từ chối:   + Hiển thị popup để chọn Lý do từ chối duyệt đk điểm bán như sau:      * + - Danh sách lý do lấy từ [Danh sách dữ liệu chung](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53023360) loại = Lý do từ chối duyệt CTTL + thêm Lý do **Khác**, trường hợp user chọn Lý do khác phải nhập thông tin Lý do (free text, 100)       * Không nhập lý do từ chối/ Không chọn option nào => Disable button "Xác nhận"     - Nhấn xác nhận:       * Điểm bán chuyển trạng thái Từ chối duyệt.       * Lưu thông tin lý do từ chối duyệt đăng ký điểm bán       * Lưu thông tin tài khoản người dùng thực hiện từ chối duyệt đk điểm bán.  * + - Ghi nhận Từ chối/Duyệt thành công, hiển thị popup, auto đóng sau 2s:  * + - Khi chọn Duyệt/Từ chối những case thỏa điều kiện thì pass; Case có lỗi hiển thị 1 popup lỗi - liệt kê tất cả các lỗi:  * + - * **Đã xảy ra lỗi**         + **Chương trình tích lũy @ tên chương trình đã bị ngưng hoạt động, vui lòng thử lại!**         + **@Mã DB- tên điểm bán đã có đăng ký Đã duyệt, vui lòng thử lại!**         + **@Mã DB- tên điểm bán đã có đăng ký Từ chối duyệt, vui lòng thử lại!**         + **@Mã DB- tên điểm bán đã có đăng ký Ngưng hoạt động, vui lòng thử lại!**         + **@Mã DB- tên điểm bán đã có đăng ký Hết hạn duyệt, vui lòng thử lại!**         + **Trường hợp có lỗi khi "Tuyến bán hàng bị ngưng hoạt động"; "Điểm bán bị ngưng hoạt động"**           - **@Mã DB-tên điểm bán có Tuyến bán hàng bị ngưng hoạt động, vui lòng thử lại!**           - **@Mã DB-tên điểm bán đã bị ngưng hoạt động, vui lòng thử lại!**         + **Không xác định được lỗi cụ thể báo msg: "Thao tác đã xảy ra lỗi, vui lòng thử lại!"** |
| **Tab Đã duyệt**  **Tab Từ chối**  **Tab Hết hạn** | * Ẩn checkbox (check/uncheck) * Thông tin điểm bán đã mô tả bên trên * Thời gian: Ghi nhận thời gian chuyển trạng thái tương ứng * Trạng thái: hiển thị trạng thái đăng ký của điểm bán tương ứng, chỉ hiển thị button  với đăng ký có trạng thái Chờ duyệt còn lại hide. * Chỉ trạng thái = Từ chối mới hiển thị lý do      * Title "Lý do:"   + Hiển thị thông tin lý do từ chối |
| Case loadding dữ liệu | 23/5/25: [Gắn rule chung load page, refresh và loadding data](https://kb.finviet.com.vn/display/DMSNEW/Manager+App) |

Chi tiết đăng ký

## Chi tiết đăng ký

| Trường dữ liệu | Mô tả |
| --- | --- |
| Trạng thái | Hiển thị trạng thái đăng ký của điểm bán tương ứng |
| Ngày, người duyệt | 23/5/25:   * Bỏ mã màu đã mô tả trong doc, về màu sắc các trạng thái có trong UI =>[gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0) * Back màn hình Chi tiết đăng ký => về màn hình Danh sách đăng ký → focus vào tab tương ứng với trạng thái trên màn hình  * Back màn hình Chi tiết chương trình => về màn hình Danh sách đăng ký → focus vào tab tương ứng với trạng thái trên màn hình  * Trạng thái:   + Chờ duyệt   + Đã duyệt     - Ngày, người duyệt (Nếu người duyệt là Tài khoản người dùng thì hiển thị Tên người dùng - Nhóm quyền)     - format: dd/mm/yyy, bởi + Họ tên người duyệt (Chức vụ)   + Từ chối:     - Ngày, người từ chối  (Nếu người từ chối là Tài khoản người dùng thì hiển thị Tên người dùng - Nhóm quyền)     - format: dd/mm/yyy, bởi + Họ tên người từ chối (Chức vụ)     - Lý do từ chối: text Title "Lý do:" + Giá trị user đã nhập.   + Hết hạn duyệt     - Ngày kết thúc đăng ký CTTL   + Ngưng hoạt động:     - Ngày, người ngưng hoạt động (tiến trình tương lai)  (Nếu là Tài khoản người dùng thì hiển thị Tên người dùng - Nhóm quyền)     - format: dd/mm/yyy, bởi + Họ tên người Ngưng (Chức vụ)     - Lý do ngưng hoạt động: text "**Lý do: Ngưng hoạt động tiến trình tích lũy**" |
|  | Label: Thông tin đăng ký  Tên chương trình tích lũy: Hiển thị dấu ... nếu dài hơn 2 dòng   * Hình ảnh điểm bán   + Trường hợp có nhiều ảnh sẽ hiển thị ảnh chụp gần nhất   + Trường hợp không có hình ảnh sẽ hiển thị hình mặc định: * Tên điểm bán: Thông tin tên điểm bán (nhiều hơn 2 dòng hiển thị dấu ba chấm) * Địa chỉ: Thông tin địa chỉ của điểm bán, (Địa chỉ điểm bán: hiển thị dấu ... nếu nhiều hơn 2 dòng)  * Ngày tạo + Ngày tạo đăng ký CTTL cho điểm bán * Bởi: Tên nhân viên - Chức vụ nhân viên đăng ký CTTL cho điểm bán * Click vào item hiển thị màn hình thông tin chi tiết điểm bán * Click vào tên chương trình hoặc dấu mũi tên  hiển thị màn hình Chi tiết chương trình |
|  | Hiển thị hình ảnh đăng ký chụp từ app saleman/ upload từ portal  Click xem hình như rule xem hình hiện tại trên app, có hiển thị timestamp với hình chụp từ app saleman đã lưu |
|  | Mốc tích lũy  Label: Mốc tích lũy  Mốc tích lũy A \* n suất: Nối chuỗi hiển thị Mốc tích lũy và số suất đăng ký của điểm bán  **Thông tin Mốc tích lũy:**  Title mốc tích lũy n:   * Số suất tối đa: Số lượng- Lấy từ Field Số suất tối đa (Tab mốc tích lũy) trên portal  * **Phần thưởng: Header**   Loại phần thưởng = Tiền thì hiển thị "Tiền thưởng".  Số tiền: Giá trị tiền tương ứng "đ"    Loại phần thường = Quà tặng thì hiển thị     * + - * Trên portal cài đặt điều kiện= Và/ Hoặc, Text sẽ thay đổi:         + ĐK và: Hiển thị text: Tất cả các sản phẩm dưới đây:         + ĐK Hoặc: *Hiển thị text:* Một trong các sản phẩm dưới đây:         + Mã sản phẩm - Tên sản phẩm  và Số lượng tương ứng (số lượng căn giữa hiển thị như hình)   Loại phần thưởng = Chiết khấu % thì hiển thị     thì hiển thị "Chiết khấu" -  Giá trị % tương ứng "%"  Tiền thưởng tối đa    * Hiển thị giá trị tiền theo field @Tiền thưởng tối đa theo cài đặt CTTL * Trường hợp không có giá trị tiền thì hiển thị text "**Không giới hạn**"     Điều kiện tích lũy   * Text highlight đỏ hiển thị theo đk "Và/ Hoặc"   + Hoặc: "Cần thỏa **một** trong các yêu cầu dưới đây:"   + Và: "Cần thỏa **tất cả** các yêu cầu dưới đây:" * Hiển thị diễn giải theo danh sách điều kiện theo setting trên portal của CTTL   + Doanh số nhóm sản phẩm đạt từ min đến max, hiển thị "đ" với số tiền. Nếu chỉ có min thì hiển thị text diễn giải: "Doanh số nhóm sản phẩm đạt từ min trở lên"   + %Doanh số nhóm sản phẩm đạt từ min đến max. Nếu chỉ có min thì hiển thị text diễn giải: "Doanh số nhóm sản phẩm đạt từ min trở lên", hiển thị 2 chữ số thập phân sau dấu chấm   + Doanh số sản phẩm đạt từ min đến max, hiển thị "đ" với số tiền. Nếu chỉ có min thì hiển thị text diễn giải: "Doanh số sản phẩm đạt từ min trở lên"   + Số lượng nhóm sản phẩm đạt từ min đến max. Nếu chỉ có min thì hiển thị text diễn giải: "Số lượng nhóm sản phẩm đạt từ min trở lên" * Hyperlink Nhóm sản phẩm:      * Title: Danh sách nhóm sản phẩm * Hiển thị text highlight theo điều kiện   + Và: Cần thỏa **tất cả** các yêu cầu dưới đây   + Hoặc: Cần thỏa **một** trong các yêu cầu dưới đây * Hiển thị các tab Nhóm n trong đó:   + n là số thứ tự theo field @Nhóm sản phẩm áp dụng n ; Thứ tự 1→ n của cài đặt portal * Tên Nhóm sản phẩm (x: count tổng sản phẩm trong nhóm) - default collapse all * expand:   + Avatar sản phẩm (ko có avatar lấy theo hình mặc định trên app ql đang có)- tên sản phẩm - Mã sản phẩm   + Vuốt lên xem nhiều hơn * Hiển thị Đã hết danh sách khi hết page   Trường hợp nhóm sản phẩm không có sản phẩm:    1/ Tất cả các nhóm trong từng tab tương ứng (Tab Nhóm 1; Tab Nhóm 2; Tab Nhóm n) đều không có sản phẩm => Chỉ hiển thị text highlight đỏ và "Không có dữ liệu" khi click vào tab Nhóm n.  2/ Nếu một vài nhóm sản phẩm trong Tab nhóm không có sản phẩm => Chỉ hiển thị tên nhóm (luôn hiển thị số 0 trong ngoặc) và ẩn icon expand/collapse   ---   Click hyperlink để xem "Sản phẩm"     * Title: **Danh sách sản phẩm** * Hiển thị text highlight theo điều kiện   + Bao gồm: Các sản phẩm **được áp dụng** trong chương trình   + Loại trừ: Các sản phẩm **không áp dụng** trong chương trình  * Avatar sản phẩm (ko có avatar lấy theo hình mặc định trên app ql đang có)- tên sản phẩm - Mã sản phẩm * Vuốt lên xem nhiều hơn * Hiển thị Đã hết danh sách khi hết page |
|  | Button Duyệt/ từ chối chỉ hiển thị khi đăng ký có trạng thái khởi tạo; những trạng thái khác ẩn   * Duyệt → Hiển thị popup thông báo: Bạn chắc chắn muốn duyệt Phiếu đăng ký đã chọn?  * + Chắc chắn:     - Đăng ký của Điểm bán chuyển sang trạng thái đk là Đã duyệt     - Lưu thông tin tài khoản người dùng thực hiện đồng ý duyệt đk điểm bán.       * Sau khi lưu thành công vẫn ở màn hình "Chi tiết đăng ký" nhưng sẽ update lại Trạng thái đăng ký & hide button Duyệt/Từ chối       * 23/05/2025: Tại màn hình Chi tiết đăng ký nếu Back màn hình Chi tiết đăng ký => về màn hình Danh sách đăng ký → focus vào tab tương ứng với trạng thái trên màn hình  * + Xem lại: Đóng popup và quay về màn hình hiện tại, vẫn giữ các checkbox đã check. * Từ chối:   + Hiển thị popup để chọn Lý do từ chối duyệt đk điểm bán như sau:      * + - Danh sách lý do lấy từ [Danh sách dữ liệu chung](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53023360) loại = Lý do từ chối duyệt CTTL + thêm Lý do **Khác**, trường hợp user chọn Lý do khác phải nhập thông tin Lý do (free text, 100)       * Không nhập lý do từ chối/ Không chọn option nào => Disable button "Xác nhận"     - Nhấn xác nhận:       * Điểm bán chuyển trạng thái Từ chối duyệt.       * Lưu thông tin lý do từ chối duyệt đăng ký điểm bán       * Lưu thông tin tài khoản người dùng thực hiện từ chối duyệt đk điểm bán.  * + - Ghi nhận Từ chối/Duyệt thành công, hiển thị popup, auto đóng sau 2s:  * + - Ghi nhận Từ chối/Duyệt thất bại có có thông tin cụ thể bị thất bại, hiển thị popup liệt kê tất cả các lỗi:     - **Đã xảy ra lỗi**       * **Chương trình tích lũy @ tên chương trình đã bị ngưng hoạt động, vui lòng thử lại!**       * **@Mã DB - tên điểm bán đã có đăng ký Đã duyệt, vui lòng thử lại!**       * **@Mã DB - tên điểm bán đã có đăng ký Từ chối duyệt, vui lòng thử lại!**       * **@Mã DB - tên điểm bán đã có đăng ký Ngưng hoạt động, vui lòng thử lại!**       * **Trường hợp có lỗi khi "Tuyến bán hàng bị ngưng hoạt động"; "Điểm bán bị ngưng hoạt động"**         + **@Mã DB - tên điểm bán có Tuyến bán hàng bị ngưng hoạt động, vui lòng thử lại!**         + **@Mã DB - tên điểm bán đã bị ngưng hoạt động, vui lòng thử lại!**     - **Không xác định được lỗi cụ thể báo msg: "Thao tác đã xảy ra lỗi, vui lòng thử lại!"** |

Chi tiết chương trình

## Chi tiết chương trình

| Trường dữ liệu | Mô tả |
| --- | --- |
| Label | Chi tiết chương trình |
| Avatar | Hình ảnh CTTL  nếu không có hình hiển thị hình default |
| item Thông tin chương trình | Trạng thái chương trình  Tên chương trình: hiển thị đầy đủ tên chương trình, không giới hạn số dòng |
|  | Nội dung chương trình: Label  Hiển thị nội dung HTML theo field @Nội dung cài đặt CTTL |
|  | Label: Mốc tích lũy  Hiển thị tất cả các Mốc tích lũy theo cài đặt CTTL  Có nhiều mốc tích lũy thì cuộn để xem  Mô tả mốc xem tại đây |

Thông tin điểm bán

## Thông tin điểm bán

| View mặc định | View thu gọn |
| --- | --- |
| Mặc định mở rộng tất cả thông tin |  |

Thông tin chi tiết điểm bán bao gồm

| Tên Trường | Mô tả |
| --- | --- |
| Thông tin điểm bán | Thông tin điểm bán bao gồm:   * Hình ảnh: Hình ảnh của điểm bán, trường hợp có nhiều hình ảnh sẽ hiển thị hình ảnh có ngày tạo gần nhất * Tên điểm bán: Thông tin tên điểm bán * Số điện thoại: Thông tin số điện thoại điểm bán * Địa chỉ: Thông tin địa chỉ điểm bán |
| Hình ảnh điểm bán | * Hình ảnh điểm bán * Có thể scroll ngang để xem hình ảnh điểm bán * Có thể chọn vào để phóng to xem hình ảnh điểm bán |
| Mã điểm bán | Hiển thị mã của điểm bán. |
| Tên điểm bán | Hiển thị tên của điểm bán. |
| Sđt điểm bán | Hiển thị số điện thoại của điểm bán. |
| Email | Hiển thị email của điểm bán. |
| Nhà Phân Phối | Hiển thị Mã NPP - Tên nhà phân phối của điểm bán |
| Tuyến bán hàng | Hiển thị tên Mã tuyến bán hàng - Tên tuyến bán hàng của điểm bán. |
| Tần suất | Hiển thị Tên bộ tần suất - Loại tần suất viếng thăm điểm bán |
| Ngày đi tuyến | Hiển thị ngày đi tuyến trong tuyến bán hàng |
| Tên chủ điểm bán | Hiển thị họ và tên của chủ điểm bán |
| Số CMND/CCCD | Hiển thị số CMND/CCCD của chủ điểm bán. |
| Ngày cấp | Hiển thị ngày cấp CMND/CCCD. Định dạng ngày: dd - MM - yyyy. |
| Nơi cấp | Hiển thị nơi cấp CMND/CCCD |
| Hộ khẩu thường trú | Hiển thị địa chỉ hộ khẩu thường trú của chủ điểm bán. |
| Vị trí điểm bán | Hiển thị thông tin vị trí điểm bán |
| Loại điểm bán | Hiển thị thông tin loại điểm bán |
| Hạng điểm bán | Hiển thị thông tin hạng điểm bán |
| Kênh bán hàng | Hiển thị thông tin kênh bán hàng của điểm bán |
| Tỉnh / Thành Phố | Hiển thị tên tỉnh hoặc thành phố của điểm bán. |
| Quận / Huyện | Hiển thị tên quận hoặc huyện của điểm bán. |
| Phường / Xã | Hiển thị tên phường hoặc xã của điểm bán. |
| Địa chỉ | Hiển thị địa chỉ chi tiết của điểm bán |
| Kinh độ | Hiển thị tọa độ kinh độ của điểm bán |
| Vĩ độ | Hiển thị tọa độ vĩ độ của điểm bán |
| Bản đồ | Hiển thị vị trí điểm bán, không điều chỉnh bản đồ |