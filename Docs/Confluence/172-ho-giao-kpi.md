none

| Target release |  |
| --- | --- |
| US | Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1725Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1722Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1720Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1721Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1724Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1719Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1717Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1716Finviet - Management System70327f0f-d77b-320c-9607-8ab3659b722fECD-1718 |
| Version | 1.0.0 |
| Document owner |  |
| Designer |  |
| Developers |  |
| QA |  |

## **Lịch sử tài liệu**

3

## **Description**

## **Requirements**

### **Xem danh sách giao KPI**

| Title | UI | Description |
| --- | --- | --- |
|  |  | 1. Đường dẫn: Quản lý chỉ tiêu → Giao KPI 2. Mô tả   Thông tin tìm kiếm:   * Tìm kiếm theo Mã | Tên phiếu giao KPI: tìm kiếm like thông tin được nhập (tối đa là 500 ký tự dạng string). Mặc định trống. Khi nhập/chọn dữ liệu xong sẽ có dấu **X** ở cuối thanh tìm kiếm, nhấn vào dấu **X** trên thanh tìm kiếm sẽ xóa điều kiện tìm kiếm khỏi ô tìm kiếm. * Đối tượng áp dụng:   + Khi nhấn vào sẽ load hết danh sách đối tượng áp dụng gồm: Nhân viên bán hàng, Giám sát bán hàng, Quản lý khu vực, Quản lý vùng, Giám đốc toàn quốc   + Cho phép chọn nhiều   + Khi nhập/chọn dữ liệu xong sẽ có dấu **X** ở cuối thanh tìm kiếm, nhấn vào dấu **X** trên thanh tìm kiếm sẽ xóa điều kiện tìm kiếm khỏi ô tìm kiếm.   + Mặc định trống. * Trạng thái:    + Gồm các trạng thái {Khởi tạo/Đã duyệt/Đã hủy}.   + Cho phép chọn nhiều trạng thái.   + Mặc định trống   + Khi nhập/chọn dữ liệu xong sẽ có dấu **X** ở cuối thanh tìm kiếm, nhấn vào dấu **X** trên thanh tìm kiếm sẽ xóa điều kiện tìm kiếm khỏi ô * Thời gian áp dụng:   + Cho phép chọn theo Ngày/Tháng   + Nếu chọn Tháng, datepicker hiển thị theo format: mm/yyyy   + Nếu chọn Ngày, datepicker hiển thị theo format: dd/mm/yyyy  * Tìm kiếm: khi nhấn vào sẽ tìm kiếm dữ liệu theo các thông tin được nhập/chọn.   Danh sách phiếu giao KPI   * Mã phiếu giao KPI: khi nhấn vào hiển thị màn hình xem chi tiết phiếu giao KPI * Tên phiếu giao KPI * Đối tượng áp dụng * Thời gian áp dụng * Trạng thái * Ngày tạo: theo định dạng dd-mm-yyyy h24:mi:ss * Ngày cập nhật: theo định dạng dd-mm-yyyy h24:mi:ss, hiển thị thời gian cập nhật gần nhất * Người tạo: hiển thị username người tạo phiếu * Người cập nhật: hiển thị username người cập nhật phiếu gần nhất * Tuỳ chỉnh   + Button Cập nhật: Chỉ hiển thị khi dòng dữ liệu đang ở trạng thái Khởi tạo   + Button Duyệt: Chỉ hiển thị khi dòng dữ liệu đang ở trạng thái Khởi tạo.   + Button Hủy: Chỉ hiển thị khi dòng dữ liệu đang ở trạng thái Khởi tạo |

### **Tạo mới phiếu giao KPI**

| Title | UI | Description |
| --- | --- | --- |
| Popup *Nhập Thông tin chung* |  | 1. Đường dẫn: Quản lý chỉ tiêu → Giao KPI → Chọn nút "Tạo mới" 2. Mô tả  * Thông tin chung   + Tên bộ KPI *[textbox]*     - Bắt buộc nhập     - Tối đa là 500 ký tự dạng string     - Mặc định trống.     - Khi nhập/chọn dữ liệu xong sẽ có dấu **X** ở cuối trường dữ liệu, nhấn vào dấu **X** sẽ xóa thông tin đã nhập/chọn khỏi trường dữ liệu.   + Vùng/Khu vực áp dụng     - Hiển thị dạng cây      * + - Chỉ chọn một     - Bắt buộc chọn     - Sau khi chọn Vùng/Khu vực => Danh sách đối tượng lọc theo   + Đối tượng áp dụng     - Bao gồm các lựa chọn: Nhân viên bán hàng, Giám sát bán hàng, Quản lý khu vực, Quản lý vùng, Giám đốc toàn quốc     - Bắt buộc nhập     - Chỉ cho phép chọn 1     - Cho phép nhập dữ liệu để tìm kiếm like thông tin     - Mặc định trống.     - Khi nhập/chọn dữ liệu xong sẽ có dấu **X** ở cuối trường dữ liệu, nhấn vào dấu **X** sẽ xóa thông tin đã nhập/chọn khỏi trường dữ liệu.   + Chỉ tiêu áp dụng      - Bắt buộc chọn.     - Mặc định trống.     - Cho phép chọn nhiều chỉ tiêu     - Hệ thống thực hiện lấy danh sách chỉ tiêu KPI có đối tượng áp dụng là giá trị được chọn tại trường Đối tượng áp dụng ~~đang ở trạng thái hoạt động~~ từ màn hình Chỉ tiêu KPI, danh sách hiển thị theo Mã chỉ tiêu     - Cho phép nhập dữ liệu để tìm kiếm like thông tin theo Mã chỉ tiêu   + Thời gian áp dụng: chọn theo ngày/tháng     - Chọn thời gian theo ngày hoặc tháng, chỉ chọn từ ngày/tháng hiện tại trở đi (chặn không được chọn ngày/tháng quá khứ)       * Nếu chọn ngày: Datepicker hiển thị ngày-tháng-năm       * Nếu chọn tháng: Datepicker hiển thị tháng-năm   + Nút Tiếp tục: Khi click vào nút → hệ thống thực hiện kiểm tra     - Nếu không nhập dữ liệu tại trường thông tin bắt buộc thì báo lỗi *"[Tên trường] là bắt buộc."* ở dưới mỗi trường dữ liệu.     - Nếu các trường thông tin hợp lệ → hệ thống thực hiện chuyển sang bước 2 - Giao KPI   + Đóng: Hiển thị popup xác nhận ngày trên nút:     - * Nếu đồng ý: hệ thống thực hiện đóng popup, không thực hiện lưu dữ liệu.       * Nếu đóng: thực hiện đóng popup xác nhận. |
| Popup *Giao KPI* |  | Mô tả:   * Nút Thêm đối tượng: Click vào nút → hệ thống thực hiện hiển thị popup Thêm đối tượng * Nút Export Excel * Nút Import Excel * Danh sách giao KPI bao gồm:   + Đối tượng: Các đối tượng áp dụng cụ thể được chọn tại popup Thêm đối tượng, hiển thị theo format: Mã nhân viên - Họ và tên nhân viên   + Các chỉ tiêu được chọn tại trường *Chỉ tiêu áp dụng* ở phần khai báo Thông tin chung   + Mục tiêu *[textbox]*:     - Không bắt buộc nhập     - Hiển thị tại từng cặp đối tượng & chỉ tiêu KPI     - Chỉ cho phép nhập ký tự số, được nhập số thập phân   + Thêm sản phẩm chỉ định *[icon button]*: Bắt buộc nhập. Chỉ hiển thị tại chỉ tiêu SKUs/đơn hàng, khi click vào nút → hệ thống hiển thị popup Thêm sản phẩm chỉ định gồm:      * + - Mã SKU:       * + Hiển thị field cho phép nhập để tìm kiếm sản phẩm         + Cho phép tìm kiếm thông tin sản phẩm like theo Mã sản phẩm, Tên sản phẩm → Danh sách sản phẩm có trạng thái "Đang hoạt động" hiển thị các thông tin trên danh sách gồm Mã SKU, Tên sản phẩm, Đơn vị (đơn vị cơ bản)         + Mặc định trống         + Cho nhập tìm kiếm tối đa 200 ký tự.         + Sản phẩm khi được chọn sẽ tự động dùng thông tin của sản phẩm đó để điền vào các field còn lại trong danh sách sản phẩm gồm: Mã SKU, Tên sản phẩm, Đơn vị tính (đơn vị tính lấy đơn vị cơ bản của sản phẩm     - Tên sản phẩm: được tự động điền vào khi có sản phẩm đã được chọn khi thêm sản phẩm vào danh sách.     - Đơn vị tính: được tự động điền vào khi có sản phẩm được chọn khi thêm sản phẩm vào danh sách.     - Nút Xóa: Click vào nút → hệ thống thực hiện xóa dòng dữ liệu sản phẩm tương ứng     - Nút Thêm sản phẩm: Khi nhấn vào sẽ thêm dòng sản phẩm để cho người dùng thêm vào danh sách sản phẩm.     - Nút Đóng: Hiển thị popup xác nhận ngày trên nút:       * Nếu đồng ý: hệ thống thực hiện đóng popup, không thực hiện lưu dữ liệu.       * Nếu đóng: thực hiện đóng popup xác nhận.     - Nút Đồng ý: Khi nhấn lưu hệ thống thực hiện kiểm tra và xử lý       * Kiểm tra         + Nếu chưa nhập các thông tin bắt buộc thì báo lỗi dưới line của trường đó với thông báo *"Trường <a> là bắt buộc"*. Với <a> là tên trường.       * Xử lý: Hiển thị popup xác nhận ngay trên nút:         + Nếu đồng ý: Hệ thống thực hiện lưu thông tin sản phẩm         + Nếu đóng: thực hiện đóng popup xác nhận. * Nút Xoá: Click vào nút → hệ thống thực hiện xoá dòng dữ liệu tương ứng * Nút Quay lại: Khi click vào nút → hệ thống thực hiện quay về màn hình khai báo Thông tin chng (khi quay lại, hệ thống vẫn giữ nguyên dữ liệu đã được nhập trước đó) * Nút Đóng: Hiển thị popup xác nhận ngày trên nút:   + - Nếu đồng ý: hệ thống thực hiện đóng popup, không thực hiện lưu dữ liệu.     - Nếu đóng: thực hiện đóng popup xác nhận. * Nút Lưu:  Khi nhấn lưu hệ thống thực hiện kiểm tra và xử lý:   + Kiểm tra:     - * Nếu không có đối tượng nào được chọn thì báo lỗi: "Vui lòng chọn ít nhất 1 đối tượng để giao KPI.".       * Nếu trong danh sách phiếu giao KPI tồn tại 1 phiếu giao KPI có đủ các điều kiện sau:         + Trạng thái: "Khởi tạo", "Đã duyệt"         + Có chứa ít nhất 1 đối tượng được giao loại chỉ tiêu trùng với phiếu giao KPI được tạo.         + Thời gian áp dụng overlap với thời gian áp dụng của phiếu giao KPI được tạo.           - Ví dụ: Hệ thống đang tồn tại phiếu giao KPI 01 (thời gian áp dụng 1/1/2025 → 31/1/2025, trạng thái Đã duyệt), nhân viên A được giao 100tr cho Sell-out tháng. Người dùng tạo phiếu giao KPI mới (thời gian áp dụng 15/1/2025 - 31/1/2025), nhân viên A được giao 150tr cho chỉ tiêu Sell-out tháng.→ Hệ thống báo lỗi: *"Không thể tạo phiếu giao KPI do đã tồn tại một phiếu khác có cùng tiêu chí áp dụng cho cùng đối tượng trong khoảng thời gian này."*   + Xử lý: Hiển thị popup xác nhận ngay trên nút:     - * Nếu đồng ý: Hệ thống thực hiện         + Tạo dòng dữ liệu phiếu giao KPI với trạng thái **Khởi tạo** tại màn hình danh sách         + Tạo mã phiếu giao KPI với chuỗi KPI + 6 ký tự số tăng dần. VD: KPI000001       * Nếu đóng: thực hiện đóng popup xác nhận.   Lưu ý: Nếu không có đối tượng được chọn, hệ thống hiển thị: *"Vui lòng thêm đối tượng trước khi thực hiện giao KPI cho đối tượng được chọn."* |
| Popup *Thêm đối tượng* |  | 1. Đường dẫn: Tại màn hình Tạo mới/Chỉnh sửa phiếu giao KPI (step 2), click vào nút Thêm đối tượng → hệ thống hiển thị popup Thêm đối tượng 2. Mô tả: Popup Thêm đối tượng bao gồm:   Thông tin tìm kiếm:   * Tìm kiếm theo Mã | Họ và tên | SĐT: tìm kiếm like thông tin được nhập (tối đa là 500 ký tự dạng string). Mặc định trống. Khi nhập/chọn dữ liệu xong sẽ có dấu **X** ở cuối thanh tìm kiếm, nhấn vào dấu **X** trên thanh tìm kiếm sẽ xóa điều kiện tìm kiếm khỏi ô tìm kiếm. * Ngày vào làm *[datepicker]*: hiển thị theo format: dd/mm/yyyy * Nút Tìm kiếm: khi nhấn vào sẽ tìm kiếm dữ liệu theo các thông tin được nhập/chọn.   Danh sách đối tượng:   * Danh sách hiển thị các nhân viên bán hàng lấy tại màn hình Nhân viên bán hàng và thoả mã các điều kiện:   + Thuộc đối tượng áp dụng đã được chọn tại trường *Đối tượng áp dụng*   + Thuộc *Vùng/Khu vực* được chọn   + Trạng thái *Đang hoạt động* * Thông tin trong danh sách bao gồm:   + Checkbox   + Mã nhân viên   + Họ và tên   + Giới tính   + Ngày sinh   + SĐT   + Chức vụ   + Ngày vào làm   Nút Đồng ý:   * Khi nhấn đồng ý hệ thống thực hiện kiểm tra và xử lý:   + Kiểm tra:     - Nếu không có đối tượng nào được chọn, báo lỗi: *"Vui lòng chọn ít nhất 1 đối tượng."*   + Xử lý: Hiển thị popup xác nhận ngay trên nút:     - Nếu đồng ý: Hệ thống thực hiện thêm đối tượng được chọn vào danh sách giao KPI tại popup Giao KPI     - Nếu đóng: thực hiện đóng popup xác nhận.  * Nút Đóng: Hiển thị popup xác nhận ngày trên nút:   + - Nếu đồng ý: hệ thống thực hiện đóng popup, không thực hiện lưu dữ liệu.     - Nếu đóng: thực hiện đóng popup xác nhận. |

### Chỉnh sửa phiếu giao KPI

| Title | UI | Description |
| --- | --- | --- |
|  |  | 1. Đường dẫn: Quản lý chỉ tiêu → Giao KPI → Chọn nút "Chỉnh sửa" một phiếu giao KPI 2. Mô tả  * "**Tạo mới phiếu giao KPI**" đổi thành "**Cập nhật phiếu giao KPI**". * Khi cập nhật sẽ cho phép sửa hết toàn bộ thông tin phiếu * Thêm nút "**Lưu & Duyệt**" tại popup Giao KPI: khi click vào nút → hệ thống thực hiện   + Cập nhật thông tin phiếu giao KPI   + Chuyển trạng thái phiếu sang **Đã duyệt**. * Sau khi lưu,hệ thống thực hiện cập nhật thông tin phiếu giao KPI   Lưu ý: Chỉ cho phép chỉnh sửa phiếu giao KPI ở trạng thái **Khởi tạo** |

### Xem chi tiết phiếu giao KPI

| Title | UI | Description |
| --- | --- | --- |
|  |  | 1. Đường dẫn: Quản lý chỉ tiêu → Giao KPI → Chọn Mã phiếu giao KPI bất kì 2. Mô tả  * Thông tin chung   + Tên bộ KPI   + Vùng/Khu vực áp dụng   + Đối tượng áp dụng   + Chỉ tiêu áp dụng   + Thời gian áp dụng   + Nút Đóng: Hiển thị popup xác nhận ngày trên nút:     - Nếu đồng ý: hệ thống thực hiện đóng popup,     - Nếu đóng: thực hiện đóng popup xác nhận.   + Nút Tiếp tục: Click vào nút → hệ thống hức hiện chuyển sang step Giao KPI * Giao KPI   + Danh sách giao KPI gồm:     - Đối tượng     - Các chỉ tiêu áp dụng     - Mục tiêu   + Nút Đóng: Hiển thị popup xác nhận ngày trên nút:     - Nếu đồng ý: hệ thống thực hiện đóng popup,     - Nếu đóng: thực hiện đóng popup xác nhận.   + Nút Quay lại: Click vào nút → hệ thống thực hiện quay lại step 1 Thông tin chung |

### Duyệt phiếu giao KPI

| Title | UI | Description |
| --- | --- | --- |
|  | N/A | 1. Đường dẫn: Quản lý chỉ tiêu → Giao KPI → Chọn "Duyệt" một phiếu giao KPI bất kì 2. Mô tả   Xử lý: Hiển thị popup "Xác nhận duyệt phiếu giao KPI" ngay trên nút:   * + Xử lý: Hiển thị popup xác nhận ngay trên nút:     - Nếu đồng ý: Hệ thống thực hiện        * Cập nhật trạng thái phiếu giao KPI       * Chuyển trạng thái phiếu sang **Đã duyệt**     - Nếu đóng: thực hiện đóng popup xác nhận.   **Lưu ý:** Chỉ được duyệt phiếu giao KPI ở trạng thái **Khởi tạo** |

### Huỷ phiếu giao KPI

| Title | UI | Description |
| --- | --- | --- |
|  | N/A | 1. Đường dẫn: Quản lý chỉ tiêu → Giao KPI → Chọn "Huỷ" một phiếu giao KPI bất kì 2. Mô tả   Khi nhấn vào hệ thống thực hiện xử lý:   * Xử lý: Hiển thị popup Xác nhận hủy phiếu giao KPI ngay trên nút:   + Nếu đồng ý: Hiển thị popup nhập Lý do hủy và bắt buộc phải nhập lý do với thông báo "Vui lòng nhập lý do":     - Nhấn Hoàn tất: Hệ thống thực hiện cập nhật trạng thái phiếu giao KPI sang **Đã hủy**     - Nhấn Hủy: Hệ thống thực hiện đóng popup, không thực hiện cập nhật.   + Nếu đóng: thực hiện đóng popup xác nhận. |

### Xóa phiếu giao KPI

| Title | UI | Description |
| --- | --- | --- |
|  | N/A | 1. Đường dẫn: Quản lý chỉ tiêu → Giao KPI → Chọn "Xóa" một phiếu giao KPI trạng thái "Khởi tạo" bất kì 2. Mô tả   Khi nhấn vào hệ thống thực hiện xử lý:   * Xử lý: Hiển thị popup Xác nhận xóa phiếu giao KPI ngay trên nút:   + Nếu đồng ý: Hiển thị popup nhập Lý do xóa và bắt buộc phải nhập lý do với thông báo "Vui lòng nhập lý do":     - Nhấn Hoàn tất: Hệ thống thực hiện xóa phiếu giao KPI     - Nhấn Hủy: Hệ thống thực hiện đóng popup, không thực hiện cập nhật.   + Nếu đóng: thực hiện đóng popup xác nhận. |

### Import

| Title | UI | Description |
| --- | --- | --- |
|  |  | 1. Đường dẫn: Tại màn hình Giao KPI, click vào nút Import Excel 2. Mô tả   Popup Import Excel bao gồm:  Nút Lấy file mẫu: Click vào nút → hệ thống thực hiện tải về template mẫu:   Nút Tải lên: Người dùng chọn vào kéo thả file import vào ô "Chọn hoặc kéo file đến vị trí này"  Nút Tiến hành xử lý:   * Mặc định disable, enable khi có file được import * Ràng buộc tối đa 10.000 dòng. * Click vào nút -> nhấn nút "Tiến hành xử lý" để import file dữ liệu vào hệ thống. Chỉ cho phép import file excel.  + Nếu file không đúng định dạng, báo lỗi: *"File import không đúng định dạng. Vui lòng kiểm tra lại"* + Nếu file không có dữ liệu, báo lỗi: "*File import không chứa dữ liệu."*  * Sau khi hệ thống thực hiện xử lý file import:   + Nếu có ít nhất 1 dữ liệu lỗi, hệ thống thực hiện show popup Chi tiết Import Giao KPI - Trạng thái "Thất bại".      * + Popup Chi tiết Import bao gồm:     - Dòng thứ: Hiển thị số thứ tự của dòng có lỗi     - Mô tả lỗi: Mô tả lỗi của từng dòng, gồm các trường hợp:       * Đối với sheet GIAO KPI:         + Nếu Mã đối tượng không tồn tại trong danh sách Nhân viên bán hàng: "*Mã đối tượng không tồn tại."*         + Nếu dòng dữ liệu không có Mã đối tượng: "*Không có Mã đối tượng."*         + Nếu Mã đối tượng của dòng dữ liệu trùng với Mã đối tượng của dòng dữ liệu khác trong file: *"Trùng Mã đối tượng với 1 dòng dữ liệu khác."*         + Nếu Mục tiêu của chỉ tiêu không phải ký tự số: *"Mục tiêu không đúng định dạng."*         + Nếu chỉ tiêu áp dụng không có trên phiếu giao KPI có điền Mục tiêu: *"Chỉ tiêu {Tên chỉ tiêu} không được áp dụng trong phiếu giao."*         + Nếu đối tượng không thuộc Vùng/Khu vực đã chọn: *"Mã đối tượng không thuộc vùng/khu vực đã chọn."*       * Đối với sheet SKU\_ORDER:         + Nếu Mã SKU không tồn tại trong danh sách sản phẩm: *"Mã SKU không tồn tại."*         + Nếu Mã đối tượng trong sheet 2 không có trong sheet 1: *"Mã đối tượng được chỉ định sản phẩm không thuộc danh sách đối tượng áp dụng."*         + Nếu mã SKU trùng với mã SKU của 1 dòng dữ liệu khác có cùng Mã đối tượng: *"Trùng Mã SKU với 1 dòng dữ liệu khác có cùng Mã đối tượng."*         + Nếu trường bắt buộc bị bỏ trống: *"Không có {Tên cột bắt buộc}."*           - Cột bắt buộc bao gồm: Mã đối tượng, Mã SKU.         + Nếu Mã SKU có trạng thái Không hoạt động: *"Mã SKU không còn hoạt động"*     - Số dòng hợp lệ: Tổng số dòng hợp lệ/Tổng số dòng dữ liệu trong file import. VD: 10/21     - Số dòng không hợp lệ: Tổng số dòng không hợp lệ/Tổng số dòng dữ liệu trong file import. VD: 11/21     - Danh sách lỗi:     - Phân trang theo 10; 50; 100   + Nếu tất cả các dữ liệu đều hợp lệ, hệ thống thực hiện thêm dữ liệu vào màn hình Giao KPI theo quy tắc sau:     - Nếu Mã đối tượng đã có trong danh sách Giao KPI và có trong file excel, hệ thống thực hiện cập nhật lại theo dữ liệu trong file excel     - Nếu Mã đối tượng chưa có trong danh sách Giao KPI nhưng có trong file excel, hệ thống thực hiện thêm mới dữ liệu trong file excel     - Nếu Mã đối tượng đã có trong danh sách Giao KPI nhưng chưa có trong file excel, hệ thống thực hiện giữ nguyên dữ liệu đó trên danh sách. |

### Export

| Title | UI | Description |
| --- | --- | --- |
| Export Excel | Button | Khi click chọn "Export excel" → Hệ thống thực hiện xuất báo cáo dưới format Excel  Nếu không có dữ liệu hoặc lỗi dữ liệu chưa được cập nhật mới nhất → Hiển thị lỗi "Không có dữ liệu để xuất file"  Tên file được định dạng: {Tên phiếu giao KPI} - [DD-MM-YYYY-HH-MM-SS]  Template Excel: TenPhieuGiaoKPI\_DDMMYYYYHHMMSS.xlsx |

### 