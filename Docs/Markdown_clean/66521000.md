|  |  |
| --- | --- |
| Issue Link |  |
| Story |  |
| Epic |  |
| Feature |  |
| Description | SD/ASM/RSM/SS đăng nhập và duyệt đăng ký tích lũy.  Duyệt 1 cập realtime |
| Document version | RedV1.0.0  15/5/25 wording các case để tránh sai sót  1/ Hiển thị logo file word/ pdf  2/ bổ sung mô tả cho gắn tài khoản thị trường/ bỏ gắn TKTT   * ~~**5/2/2026** Bỏ rule "Trường hợp Import đăng ký:  Tài khoản import có gắn Vai trò tài khoản thị trường + tài khoản thị trường (Tại màn hình tài khoản người dùng) thì mới hiển thị trên app để duyệt đăng ký"~~     3/ Không hiển thị với CTTB có loại chương trình = merchant  19/5/2025  1/ Đổi vị trí text highlight quà tặng theo điều kiện  2/ Bahavior duyệt/ từ chối  RedV1.0.1 20/5/25:  Bổ sung behavior màn hình Danh sách đăng ký   * Chức năng duyệt nhiều * Tìm kiếm điểm bán   Hiển thị popup báo lỗi dạng liệt kê tất cả các lỗi  23/5/25:   * [Gắn rule chung load page, refresh và loadding data](https://kb.finviet.com.vn/display/DMSNEW/Manager+App) * Bỏ mã màu đã mô tả trong doc, Về màu sắc các trạng thái có trong UI => [gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0) * Back màn hình Danh sách đăng ký sau khi chọn nhân viên => trở về màn hình Xét duyệt Đăng ký CTTB * Back màn hình Chi tiết đăng ký => về màn hình Danh sách đăng ký → focus vào tab tương ứng với trạng thái trên màn hình * Neo search bar nhân viên và Các tab trạng thái trên màn hình Danh sách đăng ký   RedV1.0.2  8/7/25: Chọn ngày xem CTTB → Load danh sách CTTB giao trong đoạn thời gian đã chọn  RedV1.1.0 : Chọn tìm nhân viên hiển thị màn hình Danh sách nhân viên, bỏ icon cuốn sổ, bỏ màn hình Tìm kiếm nhân viên  RedV1.2.0 : Define rule hiển thị phiếu đăng ký theo từng role login  YellowV1.2.0: Cập nhật rule hiển thị Chương trình trên APP QL |
| Document status | GreenDONE |
| Document owner |  |
| Chage History | 2 |

truenone

# Xét duyệt đăng ký CTTB

* Chức năng này yêu cầu phải thực hiện chấm công đầu ngày theo quy tắc [Rule cho các chức năng bắt buộc chấm công đầu ngày](https://kb.finviet.com.vn/display/DMSNEW/Manager+App)

* Trường hợp người dùng đã chấm công đầu ngày: Hệ thống mở màn hình như sau:

| Trường dữ liệu | Mô tả |
| --- | --- |
| Hiển thị mặc định | * Hệ thống hiển thị mặc định danh sách các CTTB:   + Ở trạng thái "Đang diễn ra": Hiển thị theo độ ưu tiên, CTTB nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình trưng bày gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Có phân quyền dữ liệu thuộc Vùng-Khu vực của CTTB và vùng-Khu vực của nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập      * + - ~~Hiển thị tất cả các CTTB  áp dụng cho các điểm bán thuộc Vùng - khu vực của tài khoản đăng nhập, dựa  vào master điểm bán của các đối tượng áp dụng để xác định Vùng - Khu vực~~       * ~~CTTB  không chọn đối tượng nào => hiểu là all vùng-Khu vực~~       * ~~CTTB  chọn Vùng -Khu vực => tức là Vùng-Khu vực CTTB  phải thuộc vùng-Khu vực user login mới hiển thị~~       * ~~CTTB  chọn NPP -> Vùng khu vực NPP phải thuộc Vùng -Khu vực user login~~       * ~~CTTB  chọn Điểm bán → Dựa vào master điểm bán mapping Vùng-Khu vực thuộc vùng-Khu vực user login để hiển thị CTTB~~       * ~~CTTB  chọn Hạng/Vị trí/ Loại điểm bán/ Kênh bán hàng => Lấy tất cả các điểm bán thuộc Hạng/Vị trí/ Loại điểm bán/ Kênh bán hàng đã chọn. Xét Vùng -khu vực của các điểm bán để hiển thị CTTB theo user login app quản lý~~   YellowV1.2.0: Cập nhật rule hiển thị Chương trình trên APP QL   Một Chương trình trưng bày (CTTB) sẽ hiển thị trên danh sách của Quản lý nếu thỏa mãn điều kiện sau:   * **Điều kiện:** Cấu hình đối tượng áp dụng của CTTB – Dựa vào Danh sách điểm bán trong các tuyến thuộc quyền quản lý của User.   + Danh sách điểm bán trong các tuyến thuộc quyền quản lý của User theo Đối tượng áp dụng của CTTB:     - Không chọn đối tượng: Tất cả các điểm bán     - Chọn Vùng/ Khu vực: Tất cả các điểm bán trong các tuyến thuộc quyền quản lý của User login và Các tuyến này thuộc vùng/ khu vực của chương trình     - Chọn NPP: Tất cả các điểm bán trong các tuyến thuộc quyền quản lý của User login và các tuyến này của NPP thuộc chương trình     - Chọn Điểm bán: điểm bán trong các tuyến thuộc quyền quản lý của User login và các điểm bán này thuộc chương trình     - **Chọn Thuộc tính** (Hạng/Vị trí/Kênh...): điểm bán trong các tuyến thuộc quyền quản lý của User login thỏa điều kiện thuộc tính của chương trình     - Chọn nhiều: Nếu một chương trình cài đặt nhiều điều kiện: Xét chỉ cần một điểm bán trên tuyến thỏa điều kiện theo Logic AND thì hiển thị   Không hiển thị CTTB có Loại chương trình = Merchant   * + Thời gian từ đầu tháng đến cuối tháng của ngày hiện tại (Ví dụ ngày hiện tại là 10/12/2024, thì lấy thời gian từ 1/12 - 31/12)   + **Thời gian lọc danh sách CTTB lấy theo thời gian chương trình, YellowV1.2.0 CTTB có thời gian chương trình có giao với thời gian bộ lọc đang chọn sẽ hiển thị.**   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item |
| Tìm kiếm | placeholder: Tìm theo Tên, Mã chương trình   * Khi người dùng nhập dữ liệu, hệ thống sẽ lọc danh sách các chương trình đang hiển thị bên dưới theo từ khóa đã nhập. * Có thể hoạt động theo kiểu nhấn nút search trên keyboard để search * 3 tab chung bộ lọc: Đang diễn ra, Ngưng hoạt động, Kết thúc |
| Bộ lọc ngày | * Thời gian từ đầu tháng đến cuối tháng của ngày hiện tại (Ví dụ ngày hiện tại là 10/12/2024, thì lấy thời gian từ 1/12 - 31/12) * Thời gian lọc danh sách CTTB   + RedV1.0.2 Chọn ngày xem CTTB → Load danh sách CTTB có thời gian chương trình giao trong đoạn thời gian đã chọn * User có thể nhấn để chọn lại thời gian      * Đến ngày >= Từ ngày * Từ ngày → Đến ngày <= 90 ngày * Không chọn ngày nào → Xác nhận → Hiển thị ngày hôm nay |
| Tabs lọc trạng thái | Các tab: "Đang diễn ra", "Ngưng hoạt động", "Kết thúc".  Hiển thị theo độ ưu tiên, CTTB nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình trưng bày gần hiện tại nhất trên cùng- trùng thì hiển thị random  **Tab Đang diễn ra:**   * Hệ thống hiển thị mặc định danh sách các CTTB:   + Ở trạng thái "Đang diễn ra"   + Có phân quyền dữ liệu thuộc Vùng-Khu vực của CTTB và vùng-Khu vực của nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Hiển thị theo độ ưu tiên, CTTB nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình trưng bày gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Thời gian lọc danh sách CTTB lấy theo ngày bắt đầu chương trình theo thời gian đã lọc bên trên   + Hiển thị tag X Phiếu chờ duyệt (đếm tất cả các phiếu chờ duyệt của CTTB)   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item         **Tab Ngưng hoạt động**   * Hệ thống hiển thị mặc định danh sách các CTTB:   + Ở trạng thái "Ngưng hoạt động"   + Có phân quyền dữ liệu thuộc Vùng-Khu vực của CTTB và vùng-Khu vực của nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Hiển thị theo độ ưu tiên, CTTB nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình trưng bày gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Thời gian lọc danh sách CTTB lấy theo ngày bắt đầu chương trình theo thời gian đã lọc bên trên   + Thẻ tag - X phiếu đăng ký: Đếm tất cả các  phiếu đã đăng ký có trạng thái (Đã duyệt; Từ chối duyệt; Hết hạn duyệt; Ngưng hoạt động)   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item   **Tab Kết thúc**   * Hệ thống hiển thị mặc định danh sách các CTTB:   + Ở trạng thái "Kết thúc"   + Có phân quyền dữ liệu thuộc Vùng-Khu vực của CTTB và vùng-Khu vực của nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Hiển thị theo độ ưu tiên, CTTB nào có độ ưu tiên càng nhỏ hiển thị trên cùng. Nếu cùng độ ưu tiên hiển thị theo ngày bắt đầu chương trình trưng bày gần hiện tại nhất trên cùng- trùng thì hiển thị random   + Thời gian lọc danh sách CTTB lấy theo ngày bắt đầu chương trình theo thời gian đã lọc bên trên   + thẻ tag - X phiếu đăng ký: Đếm tất cả các  phiếu đã đăng ký có trạng thái (Đã duyệt; Từ chối duyệt; Hết hạn duyệt; Ngưng hoạt động)   + Thông báo "Đã hết thông tin" ở cuối page   + Có phân trang hiển thị các item |
|  | Icon date: Ngày bắt đầu chương trình → Ngày kết thúc chương trình  Tên chương trình: hiển thị 2 dòng, nhiều hơn hiển thị dấu ba chấm ....  text hyperlink: Xem chi tiết → để xem Danh sách đăng ký chương trình |
|  | Icon date: Ngày bắt đầu chương trình → Ngày kết thúc chương trình  Tên chương trình: hiển thị 2 dòng, nhiều hơn hiển thị dấu ba chấm ....  text hyperlink: Xem chi tiết → để xem Danh sách đăng ký chương trình |

Danh sách dk

## Danh sách đăng ký

| Trường dữ liệu | Mô tả |
| --- | --- |
| Hiển thị mặc định | * Hệ thống hiển thị mặc định danh sách đăng ký của chương trình trưng bày đã chọn. DS điểm bán đăng ký thuộc phân quyền tất cả các nhân viên của user đăng nhập.   + Mặc định hiển thị tab Chờ duyệt   + Cho phép scroll vuốt sang phải để xem nhiều hơn; trái để xem tab trước đó   + RedV1.2.0 Có ~~người tạo~~ nhân viên chăm sóc trên tuyến bán hàng của điểm bán là nhân viên cấp dưới của tài khoản đang đăng nhập ( Chỉ mỗi SS sẽ không nhìn thấy dữ liệu của bản thân)   + Hiển thị Đã hết thông tin ở cuối page   + Có phân trang hiển thị |
| Tìm kiếm | RedV1.1.0 : Chọn tìm nhân viên hiển thị màn hình Danh sách nhân viên, bỏ icon cuốn sổ, bỏ màn hình Tìm kiếm nhân viên     * Chức năng được mô tả ở: [[Manager App] Chọn nhân viên và xem danh sách cây Salesforce nhân viên](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53035242) * Trường hợp người dùng chọn một nhân viên từ màn hình tìm kiếm, màn hình sẽ thay đổi như sau      * Danh sách yêu cầu duyệt đăng ký của điểm bán theo thời gian sẽ hiển thị theo nhân viên được chọn * Thanh search sẽ áp dụng cho tất cả các màn hình * **Tất cả nhân viên:** Quay về màn hình mặc định tất cả nhân viên có thanh search như ban đầu * **Đổi:** Chuyển tới màn hình Tìm kiếm nhân viên như mô tả bên trên để user chọn nhân viên khác. * 23/5/25:  * + Back màn hình Danh sách đăng ký sau khi chọn nhân viên => trở về màn hình Xét duyệt Đăng ký CTTB   + Neo search bar nhân viên và Các tab trạng thái trên màn hình Danh sách đăng ký   RedV1.2.0 : Define rule hiển thị phiếu đăng ký theo từng role login và chọn đổi nhân viên cấp dưới để xem dữ liệu  Phiếu đăng ký hiển thị dựa vào: Điểm bán trên tuyến của nhân viên chăm sóc nào thì hiển thị theo nhân viên chăm sóc trên tuyến đó.  **Trường hợp A: User Login là SD (Giám đốc vùng)** Vị trí cao nhất, quản lý toàn bộ RSM > ASM > SS > SM.   | Hành động chọn (Filter) | Dữ liệu hiển thị trên màn hình "Danh sách đăng ký" | | --- | --- | | **Mặc định (Tất cả)** | Toàn bộ phiếu đăng ký của **tất cả cấp dưới** (RSM, ASM, SS, SM) thuộc SD quản lý. | | **Chọn RSM** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **cấp dưới của RSM đó** phụ trách (ASM, SS, SM thuộc nhánh RSM này). | | **Chọn ASM** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **cấp dưới của ASM đó** phụ trách (SS, SM thuộc nhánh ASM này). | | **Chọn SS** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **SS và SM thuộc quyền quản lý của SS đó** phụ trách. | | **Chọn SM** | Chỉ hiển thị các phiếu đăng ký tại điểm bán do **chính SM đó** phụ trách tuyến. |   **Trường hợp B: User Login là RSM hoặc ASM**   | Hành động chọn (Filter) | Dữ liệu hiển thị trên màn hình "Danh sách đăng ký" | | --- | --- | | **Mặc định (Tất cả)** | Toàn bộ phiếu đăng ký của **tất cả cấp dưới** (SS, SM) thuộc RSM/ ASM quản lý. | | **Chọn Cấp quản lý dưới ASM** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **cấp dưới của ASM đó** phụ trách (SS, SM thuộc nhánh ASM này). | | **Chọn SS** | Toàn bộ phiếu đăng ký thuộc các điểm bán do **SS và SM thuộc quyền quản lý của SS đó** phụ trách. | | **Chọn SM** | Chỉ hiển thị các phiếu đăng ký tại điểm bán do **chính SM đó** phụ trách tuyến. |   **Trường hợp C: User Login là SS (Giám sát bán hàng)**   | Hành động chọn (Filter) | Dữ liệu hiển thị trên màn hình "Danh sách đăng ký" | Giải thích Logic | | --- | --- | --- | | **Mặc định (Tất cả)** | Toàn bộ phiếu đăng ký của **tất cả các SM** thuộc đội nhóm do SS này quản lý. | **Lưu ý:** Không hiển thị phiếu đăng ký của các điểm bán do chính SS phụ trách | | **Chọn SM** | Chỉ hiển thị các phiếu đăng ký tại điểm bán do **chính SM đó** phụ trách tuyến. | theo tuyến bán hàng của nhân viên phụ trách |     Mô tả thêm:   1. **Back:** Khi đang xem dữ liệu của một nhân viên được chọn (Ví dụ: SD đang xem SM Nguyễn Văn A), nếu nhấn nút **Back** hoặc nút **"Tất cả nhân viên"**, màn hình phải quay về trạng thái Mặc định (View tổng của SD) và reset lại thanh search. 2. **Trạng thái Tab:** Việc chọn nhân viên không làm thay đổi Tab đang đứng     1. Ví dụ: Đang đứng Tab "Chờ duyệt" và chọn nhân viên A -> Hiển thị danh sách "Chờ duyệt" của nhân viên A).    2. Các Tab khác hiển thị dữ liệu theo nhân viên được chọn |
|  | 23/5/25: Bỏ mã màu => [gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0)   Icon date: Ngày bắt đầu chương trình → Ngày kết thúc chương trình  Tên chương trình: hiển thị 2 dòng, nhiều hơn hiển thị dấu ba chấm ....  Click item hoặc dấu > để xem chi tiết chương trình |
| Tab trạng thái đăng ký | 23/5/25: Neo search bar nhân viên và Các tab trạng thái trên màn hình Danh sách đăng ký  Scroll ngang để xem các tab trạng thái  Hiển thị danh sách theo bộ lọc của nhân viên (Search bar đầu màn hình)  1/ Khi chọn nhân viên ở search bar đầu màn hình => Hiển thị tất cả các điểm bán đăng ký theo các trạng thái tương ứng của CTTB 3/ Khi chọn search bar Tên, mã điểm bán sẽ lọc lại các điểm bán có trong danh sách sẵn có. 4/ Khi đổi nhân viên khác để xem => reload lại từ đầu, load danh sách theo phân quyền nhân viên đã chọn. Khi đó text bên trong thanh search Tên, mã điểm bán sẽ bị xóa hết đi  **Tab Chờ Duyệt**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Chờ duyệt"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Đã duyệt**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Đã duyệt"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Từ Chối**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Từ chối duyệt"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Hết Hạn**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán:   + Ở trạng thái "Hết hạn duyệt"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền)   **Tab Ngưng hoạt động**   * Hệ thống hiển thị mặc định danh sách các đăng ký của điểm bán có trạng thái:   + Ở trạng thái "Ngưng hoạt động"   + Có người tạo (Người đăng ký) là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách đăng ký của điểm bán theo phân quyền user đã chọn bên trên (ko chọn user nào hiểu là tất cả user thuộc phân quyền) |
| Tìm kiếm điểm bán | placeholder: Tìm theo Tên, Mã điểm bán   * Input search: tên, mã điểm bán (Riêng cho từng tab)  * Khi nhập dữ liệu trong search bar Tên, mã điểm bán sẽ lọc lại các điểm bán có trong danh sách sẵn có bên dưới * Khi đổi nhân viên khác để xem => reload lại từ đầu, load danh sách theo phân quyền nhân viên đã chọn. Khi đó text bên trong thanh search Tên, mã điểm bán sẽ bị xóa hết đi * Có thể hoạt động: nhấn nút search trên keyboard để search   RedV1.0.1   * Khi search điểm bán hiển thị danh sách thỏa điều kiện search -> Chọn xóa text search thì load lại page * **Nút “Chọn tất cả" / "Bỏ chọn tất cả” sẽ ẩn đi** nếu người dùng đang ở trạng thái lọc, tức là khi chọn search điểm bán thì ẩn text "Chọn tất cả" / "Bỏ chọn tất cả" |
| Chọn tất cả | * Button chọn tất cả , bỏ chọn tất cả, chỉ có trong tab Chờ Duyệt (chọn tất cả item đang hiển thị) * Khi chọn tất cả sẽ hiển thị UI như sau:      * Trường hợp có >=2 điểm bán được chọn, UI sẽ hiển thị thêm 2 button Từ chối, Duyệt Tất cả   + Duyệt tất cả → Hiển thị popup thông báo: Bạn chắc chắn muốn duyệt tất cả các phiếu đã chọn?     - Chắc chắn:       * Tất cả điểm bán chuyển sang trạng thái Đã duyệt       * Lưu thông tin tài khoản người dùng thực hiện đồng ý duyệt đăng ký của điểm bán.     - Xem lại: Đóng popup và quay về màn hình hiện tại, vẫn giữ các checkbox đã check.   + Từ chối:     - Hiển thị popup để chọn Lý do từ chối duyệt đăng ký của điểm bán như sau:      * + - Danh sách lý do lấy từ [Danh sách dữ liệu chung](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53023360) loại = Lý do từ chối duyệt CTTB và thêm Lý do Khác, trường hợp user chọn Lý do khác phải nhập thông tin Lý do (free text, 100)       * 19/5/25: Không nhập lý do hoặc không chọn option nào => disable button "Xác nhận"     - Nhấn xác nhận →       * Tất cả điểm bán chuyển trạng thái Từ chối duyệt.       * Lưu thông tin lý do từ chối duyệt đăng ký của điểm bán       * Lưu thông tin tài khoản người dùng thực hiện từ chối duyệt đăng ký của điểm bán.     - User chọn nhiều điểm bán và nhấn từ chối thì Lý do được chọn sẽ áp dụng cho tất cả điểm bán được chọn từ chối duyệt. * Ghi nhận Từ chối/Duyệt thành công, hiển thị popup, auto đóng sau 2s:  * Ghi nhận Từ chối/Duyệt thất bại và không có thông tin điểm bán cụ thể bị thất bại, hiển thị popup kèm lỗi như sau:      * RedV1.0.1 Khi chọn Duyệt/Từ chối những case thỏa điều kiện thì pass; Case có lỗi  (Có điểm bán xác định bị lỗi) hiển thị 1 popup lỗi - liệt kê tất cả các lỗi: * **Đã xảy ra lỗi**   + **Chương trình trưng bày @ tên chương trình đã bị ngưng hoạt động, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Đã duyệt, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Từ chối duyệt, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Ngưng hoạt động, vui lòng thử lại!**   + **@Mã DB - tên điểm bán đã có đăng ký Hết hạn duyệt, vui lòng thử lại!**   + **"Điểm bán bị ngưng hoạt động":** Mã DB - Tên DB đã bị ngưng hoạt động. Vui lòng thử lại!   + **"Tuyến bán hàng bị ngưng hoạt động":** Mã DB - Tên DB có tuyến bán hàng ngưng hoạt động. Vui lòng thử lại!   + **Trường hợp này lỗi** khác:     - **@Mã DB - tên điểm bán đã xảy ra lỗi, vui lòng thử lại!**     Tắt popup:    * + - Những điểm bán duyệt/ từ chối thành công sẽ tự động chuyển qua các tab trạng thái tương ứng     - Duyệt/ từ chối thất bại: hiển thị highlight các lỗi inline  - Tất cả các action như search/chọn tab khác/ Pull to refresh thì clear lỗi inline. Nội dung lỗi cho từng điểm bán cụ thể **"Tuyến bán hàng đã bị ngưng hoạt động!"; "Điểm bán đã bị ngưng hoạt động!";** lỗi khác chưa define ở trên báo **"Đã xảy ra lỗi!"**        * Sau khi nhấn Chọn Tất cả → button sẽ đổi tên thành Bỏ chọn tất cả * Nhấn Bỏ Chọn → button đổi tên thành Chọn tất cả và uncheck toàn bộ trên lưới danh sách    Mô tả thêm về Text Chọn tất cả | Bỏ chọn tất cả:   * + Mặc định hiển thị: "Chọn tất cả"   + Chỉ hiển thị text "Bỏ chọn tất cả" khi tick chọn tất cả các item đang load trên màn hình     - Ví dụ:       * Load page 1: Check 1 -> 9 trên 10 item: "Chọn tất cả"       * Check 10/10: "Bỏ chọn tất cả"       * Load thêm page 2 -> check 10/20: "Chọn tất cả"   + Khi thay đổi text trong ô tìm theo điểm bán -> ẩn text "Chọn tất cả" | "Bỏ chọn tất cả"   + Khi thay đổi nhân viên -> refresh page -> hiển thị như mặc định |
| Item Điểm bán đăng ký    **Tab Chờ duyệt** | Default hiển thị uncheck  Hiển thị dữ liệu có phân trang  Sau khi check chọn hiển thị UI như hình:    -- Thông tin item điểm bán đăng ký   * Tạo ngày + Ngày tạo đăng ký CTTB cho điểm bán * Bởi: Tên nhân viên - Chức vụ nhân viên đăng ký CTTB cho điểm bán * Hình ảnh:   + Hình ảnh điểm bán   + Trường hợp có nhiều ảnh sẽ hiển thị ảnh chụp gần nhất   + Trường hợp không có hình ảnh sẽ hiển thị hình mặc định: * Tên điểm bán: Thông tin tên điểm bán (nhiều hơn 2 dòng hiển thị dấu ba chấm) * Địa chỉ: Thông tin địa chỉ của điểm bán, (Địa chỉ điểm bán: hiển thị dấu ... nếu nhiều hơn 2 dòng) * Hạn mức n - Số suất x: Nối chuỗi hiển thị hạn mức và số suất đăng ký của điểm bán * Hình ảnh: hiển thị 3 hình, nhiều hơn thì hiển thị 2 hình và ô +(n-2), icon   + Hình ảnh gồm hình chụp đăng ký app saleman; hình upload trên portal; file word/pdf- hiển thị logo word/pdf như app saleman * Chọn item hoặc dấu > hiển thị màn hình Thông tin đăng ký   ---   * Trạng thái: hiển thị trạng thái đăng ký của điểm bán, chỉ hiển thị button  với đăng ký có trạng thái Chờ duyệt * Duyệt → Hiển thị popup thông báo: Bạn chắc chắn muốn duyệt Phiếu đăng ký đã chọn??  * + Chắc chắn:     - Đăng ký của Điểm bán chuyển sang trạng thái đk là Đã duyệt     - Lưu thông tin tài khoản người dùng thực hiện đồng ý duyệt đk điểm bán.  * + Xem lại: Đóng popup và quay về màn hình hiện tại, vẫn giữ các checkbox đã check. * Từ chối:   + Hiển thị popup để chọn Lý do từ chối duyệt đk điểm bán như sau:      * + - Danh sách lý do lấy từ [Danh sách dữ liệu chung](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53023360) loại = Lý do từ chối duyệt CTTB + thêm Lý do **Khác**, trường hợp user chọn Lý do khác phải nhập thông tin Lý do (free text, 100)       * 19/5/25: Không nhập lý do hoặc không chọn option nào => disable button "Xác nhận"     - Nhấn xác nhận:       * Điểm bán chuyển trạng thái Từ chối duyệt.       * Lưu thông tin lý do từ chối duyệt đăng ký điểm bán       * Lưu thông tin tài khoản người dùng thực hiện từ chối duyệt đk điểm bán.  * + - Ghi nhận Từ chối/Duyệt thành công, hiển thị popup, auto đóng sau 2s:  * + - RedV1.0.1Ghi nhận Từ chối/Duyệt thất bại. Khi chọn Duyệt/Từ chối những case thỏa điều kiện thì pass; Case có lỗi  hiển thị 1 popup lỗi - liệt kê tất cả các lỗi:  * + - * **Đã xảy ra lỗi**         + **Chương trình trưng bày @ tên chương trình đã bị ngưng hoạt động, vui lòng thử lại!**         + **@Mã DB - tên điểm bán đã có đăng ký Đã duyệt, vui lòng thử lại!**         + **@Mã DB - tên điểm bán đã có đăng ký Từ chối duyệt, vui lòng thử lại!**         + **@Mã DB - tên điểm bán đã có đăng ký Ngưng hoạt động, vui lòng thử lại!**         + **@Mã DB - tên điểm bán đã có đăng ký Hết hạn duyệt, vui lòng thử lại!**         + **Trường hợp có lỗi khi "Tuyến bán hàng bị ngưng hoạt động"; "Điểm bán bị ngưng hoạt động"**           - **@Mã DB - tên điểm bán có Tuyến bán hàng bị ngưng hoạt động, vui lòng thử lại!**           - **"@Mã DB - tên điểm bán đã bị ngưng hoạt động, vui lòng thử lại!"**         + **Không xác định được lỗi cụ thể báo msg: "**Thao tác đã xảy ra lỗi, vui lòng thử lại!"**** |
| **Tab Đã duyệt**  **Tab Từ chối**  **Tab Hết hạn** | * Ẩn checkbox (check/uncheck) * Thông tin điểm bán đã mô tả bên trên * Thời gian: Ghi nhận thời gian chuyển trạng thái tương ứng * Trạng thái: hiển thị trạng thái đăng ký của điểm bán tương ứng, chỉ hiển thị button  với đăng ký có trạng thái Chờ duyệt còn lại hide. * Chỉ trạng thái = Từ chối mới hiển thị lý do      * Title "Lý do:"   + Hiển thị thông tin lý do từ chối |
| Các case loadding | 23/5/25: [Gắn rule chung load page, refresh và loadding data](https://kb.finviet.com.vn/display/DMSNEW/Manager+App) |

Chi tiết đăng ký

## Chi tiết đăng ký

| Trường dữ liệu | Mô tả |
| --- | --- |
| Trạng thái | Hiển thị trạng thái đăng ký của điểm bán tương ứng |
| Ngày, người duyệt | 23/5/25: Bỏ mã màu => [gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0)    * Back màn hình Chi tiết đăng ký => về màn hình Danh sách đăng ký → focus vào tab tương ứng với trạng thái trên màn hình   Trạng thái hết hạn duyệt: chỉ hiển thị ngày kết thúc đăng ký của CTTB   * Trạng thái:   + Chờ duyệt   + Đã duyệt     - Ngày, người duyệt     - format: dd/mm/yyy, bởi + Họ tên người duyệt (Chức vụ)   + Từ chối:     - Ngày, người từ chối     - format: dd/mm/yyy, bởi + Họ tên người từ chối (Chức vụ)     - Lý do từ chối: text Title "Lý do:" + Giá trị user đã nhập.   + Hết hạn duyệt     - Ngày kết thúc đăng ký CTTB   + Ngưng hoạt động:     - Ngày, người ngưng hoạt động (tiến trình tương lai)     - format: dd/mm/yyy, bởi + Họ tên người Ngưng (Chức vụ)     - Lý do ngưng hoạt động: text "Lý do: Ngưng hoạt động tiến trình trưng bày"   *(Nếu người duyệt là Tài khoản người dùng thì hiển thị Tên người dùng - Nhóm quyền)* |
|  | Label: Thông tin đăng ký  Tên chương trình trưng bày: Hiển thị dấu ... nếu dài hơn 2 dòng   * Hình ảnh điểm bán   + Trường hợp có nhiều ảnh sẽ hiển thị ảnh chụp gần nhất   + Trường hợp không có hình ảnh sẽ hiển thị hình mặc định: * Tên điểm bán: Thông tin tên điểm bán (nhiều hơn 2 dòng hiển thị dấu ba chấm) * Địa chỉ: Thông tin địa chỉ của điểm bán, (Địa chỉ điểm bán: hiển thị dấu ... nếu nhiều hơn 2 dòng)  * Ngày tạo + Ngày tạo đăng ký CTTB cho điểm bán * Bởi: Tên nhân viên - Chức vụ nhân viên đăng ký CTTB cho điểm bán * Click vào item hiển thị màn hình thông tin chi tiết điểm bán * Click vào tên chương trình hoặc dấu mũi tên  hiển thị màn hình Chi tiết chương trình |
|  | Hiển thị hình ảnh đăng ký chụp từ app saleman/ upload từ portal  Có thể xem file word/ pdf như app saleman đang xem  Click xem  hình như rule xem hình hiện tại trên app, có hiển thị timestamp với hình chụp từ app saleman đã lưu  Hiển thị cả hình ảnh lẫn logo file tại cụm Hình ảnh đăng ký.   * Click vào hình (chỉ các hình ảnh) thì như UI xem hình như hiện tại. * Click vào logo word hoặc Pdf thì hiển thị theo trình duyệt hỗ trợ đọc file |
|  | Hạn mức  Label: Hạn mức đăng ký  Hạn mức A \* n suất: Nối chuỗi hiển thị hạn mức và số suất đăng ký của điểm bán  **Thông tin hạn mức:**   * Tên hạn mức * Số suất tối đa * Tên kỳ * Xem hình mẫu: hyperlink,   + Nếu không có hình mẫu nào ẩn hyperlink text   + Hiển thị text nội dung mô tả hình ảnh (nếu có) như UI   + Nếu không có nhập mô tả hình thì không hiển thị   + click hiển thị hyperlink xem hình mẫu như sau:        * **Phần thưởng**   + Tiền thưởng: Hiển thị giá trị tiền thưởng có phân tách phần nghìn, có hiển thị đ   + Quà tặng:     - Hiển thị text theo điều kiện quà tặng Và/ Hoặc:       * **Hoặc**: Một trong các sản phẩm dưới đây       * **Và**: Tất cả các sản phẩm dưới đây:       * Mã sản phẩm - Tên sản phẩm  và Số lượng tương ứng * **Điều kiện trưng bày**  * + Số lượng hình ảnh yêu cầu: Số lượng tương ứng   + Số lần yêu cầu duyệt hình ảnh: số lượng tương ứng   + Số mặt trưng bày: số lượng tương ứng  * **Điều kiện đơn hàng**   + Trên portal cài đặt điều kiện= Và/ Hoặc, Text hiển thị sẽ thay đổi     - ĐK và => Cần thỏa tất cả các yêu cầu dưới đây     - ĐK Hoặc => Thỏa một trong các yêu cầu dưới đây   + Doanh số nhóm sản phẩm: Phương thức - Số lượng tương ứng (ví dụ: Phương thức = Lớn hơn → hiển thị "trên 10,000,000"; Phương thức = Lớn hơn hoặc bằng hiển thị "từ 10,000,000)  * + Doanh số đơn hàng: Phương thức - Số tiền tương ứng (ví dụ: Phương thức = Lớn hơn → hiển thị "trên 10,000,000"; Phương thức = Lớn hơn hoặc bằng hiển thị "từ 10,000,000)   + Số lượng nhóm sản phẩm: Phương thức - Số lượng tương ứng (ví dụ: Phương thức = Lớn hơn → hiển thị "trên 10,000,000"; Phương thức = Lớn hơn hoặc bằng hiển thị "từ 10,000,000)   Click hyperlink để xem "Nhóm sản phẩm"     * + hiển thị text theo điều kiện     - và: Cần thỏa tất cả các yêu cầu dưới đây     - Hoặc: Cần thỏa một trong các yêu cầu dưới đây   + Nhóm sản phẩm- default collapse all   + expand:     - Avatar sản phẩm - tên sản phẩm - Mã sản phẩm   + Vuốt lên xem nhiều hơn   + Hiển thị Đã hết danh sách khi hết page   Trường hợp nhóm sản phẩm không có sản phẩm:    1/ Tất cả các nhóm đều không có sản phẩm => Chỉ hiển thị text highlight đỏ và "Không có dữ liệu"  2/ Nếu một vài nhóm trong danh sách nhóm không có sản phẩm => Chỉ hiển thị tên nhóm (luôn hiển thị số 0 trong ngoặc) và ẩn icon expand/collapse |
|  | Button Duyệt/ từ chối chỉ hiển thị khi đăng ký có trạng thái khởi tạo; những trạng thái khác ẩn   * Duyệt → Hiển thị popup thông báo: Bạn chắc chắn muốn duyệt Phiếu đăng ký đã chọn??  * + Chắc chắn:     - Đăng ký của Điểm bán chuyển sang trạng thái đk là Đã duyệt     - Lưu thông tin tài khoản người dùng thực hiện đồng ý duyệt đk điểm bán.       * 19/5/25: Sau khi lưu thành công vẫn ở màn hình "Chi tiết đăng ký" nhưng sẽ update lại Trạng thái đăng ký & hide button Duyệt/Từ chối       * 23/5/25:          + Bỏ mã màu đã mô tả, các trạng thái tương ứng trên UI => [gắn link design để dev tự lấy theo UI design](https://www.figma.com/design/Max1rpXRcY1i8LhumLoBsp/Hand-off_App-Qu%E1%BA%A3n-l%C3%BD?node-id=35999-4752&p=f&t=hkbRftGSKCCBYxFw-0)         + Back màn hình Chi tiết chương trình => về màn hình Danh sách đăng ký → focus vào tab tương ứng với trạng thái trên màn hình  * + Xem lại: Đóng popup và quay về màn hình hiện tại, vẫn giữ các checkbox đã check. * Từ chối:   + Hiển thị popup để chọn Lý do từ chối duyệt đk điểm bán như sau:      * + - Danh sách lý do lấy từ [Danh sách dữ liệu chung](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53023360) loại = Lý do từ chối duyệt CTTB + thêm Lý do **Khác**, trường hợp user chọn Lý do khác phải nhập thông tin Lý do (free text, 100)       * 19/05/25: nếu không nhập lý do hoặc không chọn bất kỳ option nào thì Disable button "Xác nhận"     - Nhấn xác nhận:       * Điểm bán chuyển trạng thái Từ chối duyệt.       * Lưu thông tin lý do từ chối duyệt đăng ký điểm bán       * Lưu thông tin tài khoản người dùng thực hiện từ chối duyệt đk điểm bán.  * + - Ghi nhận Từ chối/Duyệt thành công, hiển thị popup, auto đóng sau 2s:      * Ghi nhận Từ chối/Duyệt thất bại có có thông tin cụ thể bị thất bại, hiển thị popup liệt kê tất cả các lỗi:   + **Đã xảy ra lỗi**     - **Chương trình trưng bày@ tên chương trình đã bị ngưng hoạt động, vui lòng thử lại!**     - **@Mã DB - tên điểm bán đã có đăng ký Đã duyệt, vui lòng thử lại!**     - **@Mã DB - tên điểm bán đã có đăng ký Từ chối duyệt, vui lòng thử lại!**     - **@Mã DB - tên điểm bán đã có đăng ký Ngưng hoạt động, vui lòng thử lại!**     - **Trường hợp có lỗi khi "Tuyến bán hàng bị ngưng hoạt động"; "Điểm bán bị ngưng hoạt động"**       * **@Mã DB - tên điểm bán có Tuyến bán hàng bị ngưng hoạt động, vui lòng thử lại!**       * **"@Mã DB - tên điểm bán đã bị ngưng hoạt động, vui lòng thử lại!"**     - **Không xác định được lỗi cụ thể báo msg: "Thao tác đã xảy ra lỗi, vui lòng thử lại!"** |

Thông tin chương trình

## Chi tiết chương trình

| Trường dữ liệu | Mô tả |
| --- | --- |
| Label | Chi tiết chương trình |
| Avatar | Hình ảnh CTTB  nếu không có hình hiển thị hình default |
| item Thông tin chương trình | Trạng thái chương trình  Tên chương trình: hiển thị đầy đủ tên chương trình |
|  | Nội dung chương trình: Label  Hiển thị nội dung HTML theo field @Nội dung cài đặt CTTB |
|  | Label Hạn mức tham gia  Hiển thị tất cả các hạn mức và các kỳ theo cài đặt CTTB |

Thông tin điểm bán

# Thông tin điểm bán

| View mặc định | View thu gọn |
| --- | --- |
| Mặc định mở rộng tất cả thông tin |  |

Thông tin chi tiết điểm bán bao gồm

| Tên Trường | Mô tả |
| --- | --- |
| Thông tin điểm bán | Thông tin điểm bán bao gồm:   * Hình ảnh: Hình ảnh của điểm bán, trường hợp có nhiều hình ảnh sẽ hiển thị hình ảnh có ngày tạo gần nhất * Tên điểm bán: Thông tin tên điểm bán * Số điện thoại: Thông tin số điện thoại điểm bán * Địa chỉ: Thông tin địa chỉ điểm bán |
| Hình ảnh điểm bán | * Hình ảnh điểm bán * Có thể scroll ngang để xem hình ảnh điểm bán * Có thể chọn vào để phóng to xem hình ảnh điểm bán |
| Mã điểm bán | Hiển thị mã của điểm bán. |
| Tên điểm bán | Hiển thị tên của điểm bán. |
| Sđt điểm bán | Hiển thị số điện thoại của điểm bán. |
| Email | Hiển thị email của điểm bán. |
| Nhà Phân Phối | Hiển thị Mã NPP - Tên nhà phân phối của điểm bán |
| Tuyến bán hàng | Hiển thị tên Mã tuyến bán hàng - Tên tuyến bán hàng của điểm bán. |
| Tần suất | Hiển thị Tên bộ tần suất - Loại tần suất viếng thăm điểm bán |
| Ngày đi tuyến | Hiển thị ngày đi tuyến trong tuyến bán hàng |
| Tên chủ điểm bán | Hiển thị họ và tên của chủ điểm bán |
| Số CMND/CCCD | Hiển thị số CMND/CCCD của chủ điểm bán. |
| Ngày cấp | Hiển thị ngày cấp CMND/CCCD. Định dạng ngày: dd - MM - yyyy. |
| Nơi cấp | Hiển thị nơi cấp CMND/CCCD |
| Hộ khẩu thường trú | Hiển thị địa chỉ hộ khẩu thường trú của chủ điểm bán. |
| Vị trí điểm bán | Hiển thị thông tin vị trí điểm bán |
| Loại điểm bán | Hiển thị thông tin loại điểm bán |
| Hạng điểm bán | Hiển thị thông tin hạng điểm bán |
| Kênh bán hàng | Hiển thị thông tin kênh bán hàng của điểm bán |
| Tỉnh / Thành Phố | Hiển thị tên tỉnh hoặc thành phố của điểm bán. |
| Quận / Huyện | Hiển thị tên quận hoặc huyện của điểm bán. |
| Phường / Xã | Hiển thị tên phường hoặc xã của điểm bán. |
| Địa chỉ | Hiển thị địa chỉ chi tiết của điểm bán |
| Kinh độ | Hiển thị tọa độ kinh độ của điểm bán |
| Vĩ độ | Hiển thị tọa độ vĩ độ của điểm bán |
| Bản đồ | Hiển thị vị trí điểm bán, không điều chỉnh bản đồ |

# 

# 