|  |  |
| --- | --- |
| Issue Link |  |
| Story |  |
| Epic |  |
| Feature |  |
| Description | Tính năng này cho phép nhân viên trong công ty dễ dàng tạo, theo dõi và quản lý các đơn xin nghỉ phép của mình thông qua ứng dụng di động. Đồng thời, cung cấp công cụ cho Quản lý (Manager Approve) để xem xét, phê duyệt hoặc từ chối các đơn nghỉ phép của nhân sự cấp dưới một cách nhanh chóng. |
| Document version | RedV1.0.0 |
| Document status | GreenDONE |
| Document owner |  |
| Chage History | 2 |

truenone

# Duyệt yêu cầu nghỉ phép

Đường dẫn để có thể thực hiện duyệt yêu cầu nghỉ phép: User vào menu Khác để thực hiện duyệt yêu cầu nghỉ phép

* Chức năng này yêu cầu phải thực hiện chấm công đầu ngày theo quy tắc Rule cho các chức năng bắt buộc chấm công đầu ngày

* Trường hợp người dùng đã chấm công đầu ngày, chọn "Yêu cầu nghỉ phép"
  + Hệ thống mở màn hình như sau:

* Vai trò người dùng:
  + Employee (Nhân viên): Người tạo đơn nghỉ phép, xem lịch sử và trạng thái đơn của chính mình
  + Approver (Người phê duyệt): Quản lý trực tiếp, người có quyền xem, duyệt hoặc từ chối đơn của nhân viên cấp dưới (Không được tự duyệt đơn của chính mình tạo)

Mô tả:

| Trường dữ liệu | Mô tả |
| --- | --- |
| **Tab: Lịch sử phiếu** | |
| Hiển thị mặc định | Hiển thị thông tin tổng quan về lịch sử tạo phiếu nghỉ phép của chính nhân viên đang đăng nhập (Hiển thị danh sách yêu cầu nghỉ phép được tạo bởi nhân viên)  Gom nhóm các phiếu trong tháng  Mặc định hiển thị tháng hiện tại |
| Từ ngày  - đến ngày | * Thời gian từ đầu tháng đến cuối tháng của ngày hiện tại (Ví dụ ngày hiện tại là 10/12/2024, thì lấy thời gian từ 1/12 - 31/12) * Thời gian lọc danh sách yêu cầu nghỉ phép lấy theo ngày tạo yêu cầu nghỉ phép. * User có thể nhấn để chọn lại thời gian      * Đến ngày >= Từ ngày * Từ ngày → Đến ngày không giới hạn |
| Bộ lọc | Chọn icon lọc hiển thị màn hình:    **1/ Trạng thái:** Cho phép lọc các trạng thái phiếu, được chọn nhiều trạng thái để lọc:   * Danh sách trạng thái bao gồm:   + Chờ duyệt   + Đã duyệt   + Từ chối   + Đã hủy  * **Kết quả lọc:** Danh sách yêu cầu nghỉ phép sẽ tự động được lọc để hiển thị những yêu cầu nghỉ phép thuộc trạng thái đã chọn. * **Xóa lựa chọn:** Người dùng có thể chọn/bỏ chọn trong danh sách để chọn/bỏ chọn trạng thái không mong muốn. * Trường hợp bỏ chọn tất cả trạng thái thì mặc định hiểu là chọn tất cả trạng thái để tìm kiếm. * Khi mở màn hình lọc mặc định không chọn dữ liệu nào.   **2/ Áp dụng:**  **Chức năng:**   * Nút "Áp dụng" cho phép người dùng thực hiện áp dụng các bộ lọc đã chọn trên danh sách yêu cầu nghỉ phép. * Khi người dùng đã thiết lập các bộ lọc, nút "Áp dụng" sẽ kích hoạt chức năng lọc danh sách yêu cầu nghỉ phép theo các tiêu chí đã chọn.   **Cách sử dụng:**   1. **Thiết lập bộ lọc:** Người dùng chọn các tiêu chí trên bộ lọc. 2. **Nhấp vào nút:** Sau khi thiết lập, người dùng nhấp vào nút "Áp dụng" để áp dụng các bộ lọc và hiển thị kết quả Áp dụng trong danh sách yêu cầu nghỉ phép. 3. **Hiển thị kết quả:** Danh sách yêu cầu nghỉ phép sẽ cập nhật và hiển thị các yêu cầu nghỉ phép phù hợp với các tiêu chí đã chọn.   **Lưu ý:**Nếu không có tiêu chí nào được chọn, danh sách yêu cầu nghỉ phép sẽ không thay đổi khi nhấn nút "Áp dụng".  **3/ Đặt lại:** Nút "Đặt lại" cho phép người dùng xóa mọi bộ lọc hiện tại, đồng thời trả về trạng thái ban đầu của màn hình lọc |
| **Danh sách** | |
| Tháng - Năm | Hiển thị tháng - năm gần nhất có tạo yêu cầu nghỉ phép của nhân viên đang đăng nhập  Gom nhóm những yêu cầu nghỉ phép có ngày nghỉ trong tháng vào cùng 1 tháng của năm |
| Tạo lúc  Bởi | Tạo lúc: Hiển thị Giờ phút, ngày tháng năm tạo phiếu (Tạo lúc 09:35, 22/11/2024)  Bởi: Hiển thị Tên nhân viên - Mã nhân viên - Chức vụ nhân viên tạo yêu cầu nghỉ phép |
| Mã yêu cầu | Hiển thị mã yêu cầu hệ thống tự gen tương ứng khi lưu |
| Ngày | Hiển thị ngày nghỉ phép trên yêu cầu nghỉ phép  Format: DD-MM-YYYY → DD-MM-YYYY  Trường hợp Từ ngày = Đến ngày thì chỉ hiển thị 1 ngày kiểu DD-MM-YYYY |
| Loại nghỉ phép | Hiển thị loại nghỉ phép tương ứng với mỗi phiếu nghỉ phép.  Trường hợp Loại nghỉ phép dài hơn 2 dòng → hiển thị 2 dòng + dấu "...".   * Khi chọn mở rộng sẽ hiển thị full giá trị đã nhập |
| Trạng thái | Hiển thị trạng thái yêu cầu nghỉ phép  Dòng chữ trạng thái   * Đỏ: Đã hủy * Xanh: Đã duyệt * Đỏ nhạt: Từ chối * Đen: Chờ duyệt |
| Xem thêm | Chọn "Xem thêm" → mở rộng phiếu được chọn, như hình:   * "Xem thêm + mũi tên hướng xuống" chuyển thành "Thu gọn + Mũi tên hướng lên" * Chọn "Thu gọn" Trở lại trạng thái thu gọn ban đầu * Chọn Cụm "Số ngày nghỉ" hiển thị chi tiết ngày nghỉ |
| Mở rộng tất cả | Khi chọn "Mở rộng tất cả":   * Tất cả các phiếu nghỉ phép trong tháng sẽ mở rộng như hình: * Dòng chữ "Mở rộng tất cả" đổi thành "Thu gọn tất cả" * Tất cả "Xem thêm+ mũi tên hướng xuống" trở thành "Thu gọn+ mũi tên hướng lên"   Chọn "Thu gọn tất cả" →Thu gọn tất cả các component về lại trạng thái ban đầu. "Thu gọn tất cả" đổi thành "Mở rộng tất cả" |
| Số ngày nghỉ | Hiển thị tổng số ngày nghỉ.  Ví dụ: 1; 2; 0.5; 1.5; 2.5   * Click vào số ngày nghỉ hoặc dấu mũi tên hướng xuống → expand danh sách các ngày cụ thể và ngày nghỉ tương ứng với mỗi ngày. Hiển thị ngày gần hiện tại lên đầu và lý do nghỉ, như hình:      * Chọn ngược lại, click vào số ngày nghỉ hoặc dấu mũi tên hướng lên → thu gọn về lại trạng thái ban đầu. |
| Lý do nghỉ | Hiển thị lý do nghỉ phép trên yêu cầu nghỉ phép |
| Lý do khởi tạo lại | Chỉ hiển thị **trên phiếu có trạng thái = "Chờ duyệt" & phiếu đã được chuyển trạng thái từ Đã duyệt → Chờ duyệt** (trên portal: [HT] Duyệt yêu cầu nghỉ phép → hiển thị lý do Khởi tạo lại yêu cầu)   * Còn lại ẩn cụm thông tin này. |
| Duyệt lúc  Bởi | Hiển thị người xử lý gần nhất tại thời điểm mở màn hình gồm Thời gian cập nhật HH:MM, DD/MM/YYYY  Bởi: Người cập nhật, hiển thị họ và tên (mã chức vụ) nếu là SS/ASM/RSM/SD. Nếu người xử lý là Admin portal thì chỉ hiển thị Họ và tên người duyệt |
| Lý do từ chối/ Lý do hủy yêu cầu | Luôn hiển thị lý do = **"Giá trị người xử lý đã cập nhật gần nhất" [HT] Duyệt yêu cầu nghỉ phép**  tại thời điểm mở màn hình   * ĐÃ HỦY: Trạng thái Chờ duyệt → chuyển sang **Đã hủy** do người tạo hủy: **"Giá trị người xử lý đã cập nhật gần nhất" =** hiển thị lý do hủy yêu cầu * TỪ CHỐI: Trạng thái Chờ duyệt → chuyển sang **Từ chối** do người duyệt xử lý Từ chối: **"Giá trị người xử lý đã cập nhật gần nhất" =** Hiển thị lý do từ chối yêu cầu |
| **Button xử lý:** | |
| Hủy yêu cầu | Button này chỉ xuất hiện ở những phiếu nghỉ phép có**trạng thái "Chờ duyệt"**  Chọn vào button --> Hiển thị popup: Bạn chắc chắn Hủy yêu cầu nghỉ phép này?     * Chắc chắn:      * + Hiển thị popup nhập lý do hủy yêu cầu: Freetext (300). Bắt buộc phải nhập "Lý do hủy yêu cầu"   + Xác nhận: Hệ thống sẽ tự động thay đổi trạng thái của yêu cầu nghỉ phép từ "Chờ duyệt" sang "Đã hủy" → Thông báo: Hủy yêu cầu nghỉ phép thành công! → Lưu thông tin Ngày cập nhật, Người cập nhật, Lý do hủy yêu cầu. [[HT] Duyệt yêu cầu nghỉ phép](https://kb.finviet.com.vn/pages/viewpage.action?pageId=92779886)   + Nhấn X: Tắt popup và quay về màn hình hiện tại  * Trở lại -> Đóng popup quay về màn hình hiện tại |
| Chỉnh sửa | Chỉ những phiếu nghỉ phép có **trạng thái "Chờ duyệt" &  ngày tạo -> ngày hiện tại cách nhau 30 ngày** hiển thị icon điều chỉnh. Chọn hiển thị màn hình:    Hiển thị tất cả thông tin của phiếu nghỉ phép đã tạo, cho phép điều chỉnh tất cả trên phiếu:   * Title: Chỉnh sửa yêu cầu nghỉ phép * Được phép chỉnh sửa tất cả trên phiếu nghỉ phép * Chọn Xác nhận để Cập nhật yêu cầu nghỉ phép; Hiển thị thông báo xác nhận lưu thành công và ghi nhận người cập nhật ; ngày cập nhật và ghi đè nội dung của phiếu này. * Chọn back trở ra để hủy bỏ thao tác chỉnh sửa yêu cầu. Hiển thị[Cảnh báo trước khi thoát](https://kb.finviet.com.vn/display/DMSNEW/Manager+App?src=contextnavpagetreemode) |
| Tạo yêu cầu | Chọn button "Tạo yêu cầu" → hiển thị màn hình tạo mới yêu cầu nghỉ phép |
| Button back | * Chọn thoát màn hình * Hiển thị[Cảnh báo trước khi thoát](https://kb.finviet.com.vn/display/DMSNEW/Manager+App?src=contextnavpagetreemode) |
| Loại nghỉ phép | Mặc định hiển thị placeholder: Chọn Loại nghỉ phép, bắt buộc phải chọn loại nghỉ phép.  **Popup Loại nghỉ phép:**    Loại nghỉ phép lấy từ Dữ liệu chung ([Danh sách dữ liệu chung](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53023360)). Chọn Loại nghỉ phép được set từ Dữ liệu chung.   Bắt buộc phải cấu hình Loại nghỉ phép này. Nếu chưa cấu hình một loại nghỉ phép nào thì khi chọn selectbox hiển thị msg: "Chưa cấu hình dữ liệu loại nghỉ phép" |
| Thời gian nghỉ phép | * **Thời gian nghỉ:** Hiển thị mặc định Từ ngày = Đến ngày = Ngày hiện tại. Format: dd/mm/yyyy   + User có thể chọn lại. Bắt buộc phải chọn ngày   + Đến ngày >= Từ ngày   + Có thể chọn Từ ngày, đến ngày thuộc ngày quá khứ, vì cho trường hợp nhân viên nghỉ trước và tạo yêu cầu sau     - **Validate Từ ngày → Đến ngày:**       * **Đến ngày ≥ Từ ngày: Đến ngày phải lớn hơn hoặc bằng Từ ngày**       * **Từ ngày ≥ Ngày hiện tại − 30 ngày: Từ ngày không được trước ngày hiện tại quá 30 ngày**       * **Đến ngày ≤ Ngày hiện tại + 30 ngày: Đến ngày không được sau ngày hiện tại quá 30 ngày**       * **Đến ngày − Từ ngày ≤ 60 ngày: Khoảng thời gian không được vượt quá 60 ngày** * **Số ngày nghỉ:** Mặc định hiển thị 1 * Danh sách nghỉ mặc định Collapse; Thu gọn, người dùng chọn để Mở rộng xem danh sách lịch bên dưới  ---  Sau khi người dùng chọn **Từ ngày → Đến ngày**, hệ thống tự động gen danh sách lịch nghỉ nhưng ở trạng thái Collapse.  Chọn dấu mũi tên xuống → Hiển thị Danh sách lịch nghỉ và dấu mũi tên hướng lên. Và ngược lại.  Label: Danh sách lịch nghỉ   1. **Generate danh sách ngày** = tất cả các ngày trong khoảng [Từ ngày, Đến ngày]; trừ ngày nghỉ lễ theo cấu hình ngày công [[HO] Cấu hình chấm công](https://kb.finviet.com.vn/pages/viewpage.action?pageId=48440329) 2. **Mặc định mỗi ngày = 1** (nguyên ngày). Cho phép chọn lại 0.5 hoặc 1 3. **Tự động tính tổng "Số ngày nghỉ"** = sum tất cả giá trị đã chọn → hiển thị**"Số ngày nghỉ"** 4. **Từ ngày = Đến ngày** → List chỉ có 1 ngày, default = 1   1/6/20226: Thêm ví dụ các case Generate Danh sách lịch nghỉ  Sau khi chọn Từ ngày- Đến ngày, Hệ thống kiểm tra ngày làm việc và gen ra danh sách lịch nghỉ. Danh sách lịch nghỉ được tạo ra chỉ bao gồm các ngày làm việc thực tế rơi vào khoảng thời gian người dùng đã chọn. Những ngày Thứ 7, CN hoặc Ngày lễ sẽ không xuất hiện trong danh sách này.  **Bảng Tổng Hợp Các Ví Dụ Chọn Ngày Nghỉ Phép**   | STT | **Case điển hình** | **Khoảng ngày chọn (Từ ngày → Đến ngày)** | **Các ngày hệ thống quét qua** | **Danh sách ngày nghỉ được Generate (Chỉ giữ lại ngày làm việc)** | **Số ngày nghỉ (ngày)** | **Kết quả** | | --- | --- | --- | --- | --- | --- | --- | | **1** | **Rơi vào 1 ngày làm việc** | 12/03/2026 (Thứ 5) | 12/03 (Thứ 5) | Nếu ngày làm việc → 12/03/2026 (Mặc định: 1 ngày) | **1** | **Số ngày nghỉ 1**  Hệ thống chỉ hiển thị cho 1 ngày đi làm thực tế.  Trạng thái mặc định Collapse  Cho phép người dùng bấm gửi hoặc sửa thành 0.5. | | **2** | **Rơi vào ngày nghỉ cuối tuần** | 14/03/2026 (Thứ 7) | 14/03 (Thứ 7) | (Trống - Hệ thống tự động lọc bỏ Thứ 7) | **0** | Nút Xác nhận bị disable. | | **3** | **Rơi vào 1 ngày nghỉ Lễ** | 30/04/2026 (Thứ 5) | 30/04 (Thứ 5) | (Trống - Hệ thống lọc bỏ do trùng lịch Lễ 30/4) | **0** | Nút Xác nhận bị disable. | | **4** | **Nhiều ngày (Bao gồm cả ngày nghỉ tuần)** | 10/03/2026 (Thứ 3) →  16/03/2026 (Thứ 2) | 10/03, 11/03, 12/03, 13/03, 14/03, 15/03, 16/03 | - 10/03/2026 (1)  - 11/03/2026 (1)  - 12/03/2026 (1)  - 13/03/2026 (1)  (Loại bỏ 14/03 và 15/03)  - 16/03/2026 (1) | **5** | **Số ngày nghỉ 5**  Hệ thống chỉ hiển thị cho 5 ngày đi làm thực tế.  Trạng thái mặc định Collapse  Cho phép người dùng bấm gửi hoặc sửa thành 0.5 với mỗi ngày | | **5** | **Từ ngày Thứ 7 đến ngày Chủ Nhật** | 14/03/2026 (Thứ 7) → 15/03/2026 (Chủ Nhật) | 14/03 (Thứ 7), 15/03 (CN) | (Trống - Cả 2 ngày đều bị loại bỏ) | **0** | Nút Xác nhận bị disable. | | **6** | **Nhiều ngày (Trúng kỳ nghỉ Lễ trong tuần)** | 29/04/2026 (Thứ 4) →  04/05/2026 (Thứ 2) | 29/04 (T4),  30/04 (T5),  01/05 (T6),  02/05 (T7),  03/05 (CN),  04/05 (T2), | - 29/04/2026 (1)  - 04/05/2026 (1)  (Loại bỏ ngày lễ 30/04, 01/05 và ngày nghỉ cuối tuần 02/05, 03/05) | **2** | **Số ngày nghỉ 2**  Hệ thống chỉ hiển thị cho 2 ngày đi làm thực tế.  Trạng thái mặc định Collapse  Cho phép người dùng bấm gửi hoặc sửa thành 0.5 với mỗi ngày nghỉ |   **Trường hợp chọn ngày không phải ngày làm việc như các case đã ví dụ => Hiển thị Msg:”Ngày nghỉ phép phải thuộc ngày làm việc. Vui lòng kiểm tra lại!”**    Behavior khi thay đổi date range   |  |  | | --- | --- | | User mở rộng range (thêm ngày) | Thêm ngày mới vào cuối list/ đầu list theo thứ tự ngày, mặc định = **1** | | User thu hẹp range (bớt ngày) | Xóa các ngày không còn trong range khỏi list | | User đổi hoàn toàn range | Reset toàn bộ list, generate lại từ đầu, mặc định = **1** | |
| Lý do nghỉ phép | Label: Lý do  Placeholder: Nhập nội dung. User bắt buộc nhập lý do nghỉ phép (tối đa 300 ký tự).  Không hiển thị label Lý do nghỉ phép, chỉ hiển thị Placeholder: Lý do nghỉ |
| Xác nhận | User nhấn vào trường này hệ thống sẽ thực hiện kiểm tra  **1. Tồn tại chấm công:**   * Trường hợp ngày nghỉ phép mà nhân viên tạo đã có tồn tại chấm công đầu ngày, hệ thống hiển thị cảnh báo:   + Ngày @ngày phép1, @ngày phép 2 đã có thông tin chấm công đầu ngày, bạn vẫn muốn tạo nghỉ phép!     - Tạo nghỉ phép: Vẫn tiếp tục tạo nghỉ phép và không cần quan tâm đến thông tin chấm công     - Trở lại: Tắt popup và quay về màn hình trước đó.   **2. Trùng thời gian nghỉ phép:**  Hệ thống thực hiện kiểm tra trong thời gian được chọn có tồn tại yêu cầu nghỉ phép đang ở trạng thái "Chờ duyệt" hay "Đã duyệt" hay không. Nếu có thì thông báo: Đã có yêu cầu nghỉ phép @ngày phép trong thời gian được chọn, vui lòng chọn thời gian khác.  VD: Đã có yêu cầu nghỉ phép ngày 22/11/2024 trong thời gian được chọn, vui lòng chọn thời gian khác.  =====> Nếu thỏa hết các điều kiện trên thì hiển thị [cảnh báo trước khi lưu dữ liệu](https://kb.finviet.com.vn/display/DMSNEW/Salesman+App)   * Trở lại: Tắt popup và ở lại màn hình hiện tại * Đồng ý → yêu cầu nghỉ phép được tạo và chuyển sang trạng thái Chờ duyệt  * Format mã yêu cầu nghỉ phép: Hệ thống tự động tạo theo format: OFF + DDMMYYYYHHMMSS tại thời điểm submit phiếu * Người tạo yêu cầu nghỉ phép sẽ được ghi nhận cho chính tài khoản người dùng đang đăng nhập. |
|  | |
| **Tab: Duyệt yêu cầu** | |
| Hiển thị mặc định | * Hệ thống hiển thị mặc định danh sách các yêu cầu nghỉ phép **ở Tab "Chờ duyệt"**:   + Tab "Chờ duyệt".     - Hiển thị tất cả các yêu cầu có người tạo là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Tab: Đã duyệt, Từ chối: Chỉ lấy những yêu cầu nghỉ phép có người chuyển trạng thái (Người xử lý duyệt/ từ chối) là tài khoản nhân viên đang đăng nhập (nghĩa là chỉ hiển thị những yêu cầu nghỉ phép do nhân viên đang đăng nhập xét duyệt)   + Thời gian từ đầu tháng đến cuối tháng của ngày hiện tại (Ví dụ ngày hiện tại là 10/12/2024, thì lấy thời gian từ 1/12 - 31/12)   + Thời gian lọc danh sách yêu cầu nghỉ phép lấy theo ngày tạo yêu cầu nghỉ phép. |
| Thông tin nhân viên | UI mặc định:     * Chọn vào button Đổi để thực hiện tìm và chọn một nhân viên khác để xem dữ liệu, hệ thống hiển thị màn hình [Danh sách nhân viên](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53035242#id-[ManagerApp]Ch%E1%BB%8Dnnh%C3%A2nvi%C3%AAnv%C3%A0xemdanhs%C3%A1chc%C3%A2ySalesforcenh%C3%A2nvi%C3%AAn-List_User) * Sau khi đã đổi nhân viên → UI hiển thị:    + Chọn button Đổi → như trên: Dũ liệu trên màn hình hiển thị theo người tạo là người được chọn.   + Chọn "Xem thông tin của tôi": Quay về UI tất cả nhân viên của chính mình như ban đầu.  * **Chức năng được mô tả ở: [[Manager App] Chọn nhân viên và xem danh sách cây Salesforce nhân viên](https://kb.finviet.com.vn/pages/viewpage.action?pageId=53035242)** |
| Thời gian | * Thời gian từ đầu tháng đến cuối tháng của ngày hiện tại (Ví dụ ngày hiện tại là 10/12/2024, thì lấy thời gian từ 1/12 - 31/12) * Thời gian lọc danh sách yêu cầu nghỉ phép lấy theo ngày tạo yêu cầu nghỉ phép. * User có thể nhấn để chọn lại thời gian      * Đến ngày >= Từ ngày * Từ ngày → Đến ngày không giới hạn   *Lưu ý: Khi vuốt màn hình lên → Neo Thời gian và ẩn thông tin nhân viên đang xem, như hình:* |
| Tab Chờ duyệt, Đã duyệt, Từ chối | **Tab Chờ Duyệt**   * Hệ thống hiển thị mặc định danh sách các yêu cầu nghỉ phép:   + Ở trạng thái "Chờ duyệt"   + **Có người tạo là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập**   + Danh sách yêu cầu nghỉ phép có ngày tạo theo thời gian đã chọn bên trên. Hiển thị theo ngày tạo gần nhất lên đầu.   **Tab Đã duyệt**   * Hệ thống hiển thị mặc định danh sách các yêu cầu nghỉ phép:   + Ở trạng thái "Đã duyệt"   + Có người tạo là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách yêu cầu nghỉ phép có ngày tạo theo thời gian đã chọn bên trên.   + **Yêu cầu nghỉ phép có người chuyển trạng thái là tài khoản nhân viên đang đăng nhập (nghĩa là chỉ hiển thị những yêu cầu nghỉ phép do nhân viên đang đăng nhập xét duyệt chấp nhận)**   **Tab Từ chối**   * Hệ thống hiển thị mặc định danh sách các yêu cầu nghỉ phép:   + Ở trạng thái "Từ chối"   + Có người tạo là nhân viên cấp dưới của tài khoản nhân viên đang đăng nhập   + Danh sách yêu cầu nghỉ phép có ngày tạo theo thời gian đã chọn bên trên.   + **Yêu cầu nghỉ phép có người chuyển trạng thái là tài khoản nhân viên đang đăng nhập (nghĩa là chỉ hiển thị những yêu cầu nghỉ phép do nhân viên đang đăng nhập xét duyệt Từ chối)** |
| **Component**  **Component "Thông tin phiếu nghỉ phép"** | | Trường dữ liệu | Mô tả | | --- | --- | | Tạo lúc  Bởi | Tạo lúc: Hiển thị Giờ phút, ngày tháng năm tạo phiếu (Tạo lúc 09:35, 22/11/2024)  Bởi: Hiển thị Tên nhân viên - Mã nhân viên - Chức vụ nhân viên tạo yêu cầu nghỉ phép | | Mã yêu cầu | Hiển thị mã yêu cầu hệ thống tự gen tương ứng khi lưu | | Ngày | Hiển thị ngày nghỉ phép trên yêu cầu nghỉ phép  Format: DD-MM-YYYY → DD-MM-YYYY  Trường hợp Từ ngày = Đến ngày thì chỉ hiển thị 1 ngày kiểu DD-MM-YYYY | | Loại nghỉ phép | Hiển thị loại nghỉ phép tương ứng với mỗi phiếu nghỉ phép.  Trường hợp Loại nghỉ phép dài hơn 2 dòng → hiển thị 2 dòng + dấu "..."   * Khi mở rộng sẽ hiển thị full nội dung này. | | Trạng thái | Hiển thị trạng thái yêu cầu nghỉ phép  Dòng chữ trạng thái   * Xanh: Đã duyệt * Đỏ nhạt: Từ chối * Đen: Chờ duyệt | | Xem thêm | Chọn "Xem thêm" → mở rộng phiếu được chọn, như hình:   * "Xem thêm + mũi tên hướng xuống" chuyển thành "Thu gọn + Mũi tên hướng lên" * Chọn "Thu gọn" Trở lại trạng thái thu gọn ban đầu * Chọn Cụm "Số ngày nghỉ" hiển thị chi tiết ngày nghỉ | | Số ngày nghỉ | Hiển thị tổng số ngày nghỉ.  Ví dụ: 1; 2; 0.5; 1.5; 2.5   * Click vào số ngày nghỉ hoặc dấu mũi tên hướng xuống → expand danh sách các ngày cụ thể và ngày nghỉ tương ứng với mỗi ngày. Hiển thị ngày gần hiện tại lên đầu và lý do nghỉ, như hình:       * Chọn ngược lại, click vào số ngày nghỉ hoặc dấu mũi tên hướng lên → thu gọn về lại trạng thái ban đầu. | | Lý do nghỉ | Hiển thị lý do nghỉ phép trên yêu cầu nghỉ phép | | Lý do khởi tạo lại | Chỉ hiển thị trên **Tab "Chờ duyệt" với những phiếu đã được chuyển trạng thái từ Đã duyệt → Chờ duyệt** (trên portal: [HT] Duyệt yêu cầu nghỉ phép → Lý do khởi tạo lại)   * Còn lại ẩn cụm thông tin này. | | Thời gian xử lý | Hiển thị Thời gian cập nhật HH:MM, DD/MM/YYYY khi người dùng từ chối duyệt/ Duyệt | | Lý do từ chối duyệt | Chỉ hiển thị ở trạng thái "Từ chối"  Chỉ hiển thị giá trị người dùng nhập khi từ chối duyệt | |
| Chọn tất cả | * **Chọn tất cả các yêu cầu nghỉ phép cần duyệt trên tab Chờ Duyệt**  * Button này chỉ hiển thị ở tab Chờ duyệt * Khi chọn tất cả sẽ hiển thị UI như sau:      * Checkbox chọn chỉ hiển thị với những yêu cầu nghỉ phép   + **Chỉ được chọn các dòng có trạng thái "Chờ duyệt"; các trạng thái khác sẽ bị disable/ Ẩn**   + **Chỉ cho chọn các phiếu có ngày tạo -> ngày hiện tại cách nhau 30 ngày** * Trường hợp có >=2 yêu cầu nghỉ phép được chọn, UI sẽ hiển thị thêm 2 button Từ chối, Duyệt tất cả   + Duyệt tất cả: Khi người dùng nhấn vào button này, hệ thống hiển thị xác nhận: Bạn chắc chắn muốn duyệt tất cả các phiếu đã chọn?  * + - Chắc chắn: Hệ thống sẽ tự động thay đổi trạng thái của **TẤT CẢ** các yêu cầu nghỉ phép đang được chọn (đã đánh dấu check) từ "Chờ duyệt" sang "Đã duyệt" → Thông báo: Ghi nhận thành công! → Lưu thông tin Ngày cập nhật, Người cập nhật     - Xem lại: Tắt popup và quay về màn hình hiện tại   + Từ chối: Khi người dùng nhấn vào button này, hệ thống hiển thị xác nhận: Bạn chắc chắn từ chối duyệt tất cả các phiếu đã chọn?     - Chắc chắn:       * Hiển thị popup nhập lý do từ chối: Label: Lý do từ chối duyệt, bắt buộc nhập. Placeholder: Nhập tối đa 300 ký tự.          + Xác nhận: Hệ thống sẽ tự động thay đổi trạng thái của **TẤT CẢ** các yêu cầu nghỉ phép đang được chọn (đã đánh dấu check) từ "Chờ duyệt" sang "Từ chối" → Thông báo: Ghi nhận thành công! → Lưu thông tin Ngày cập nhật, Người cập nhật, Lý do từ chối (Áp dụng lý do cho tất cả các dòng được chọn)         + Chọn dấu 'x': Tắt popup và quay về màn hình hiện tại.     - Xem lại: Tắt popup và quay về màn hình hiện tại  * Ghi nhận Từ chốt/Duyệt thành công, hiển thị popup, auto đóng sau 2s:  * Ghi nhận Từ chốt/Duyệt thất bại, hiển thị popup kèm lỗi như sau:      * Sau khi nhấn Chọn Tất cả → button sẽ đổi tên thành Bỏ chọn. * Nhấn Bỏ Chọn → button đổi tên thành Chọn tất cả và uncheck toàn bộ trên lưới danh sách |
| **Tab Chờ Duyệt** | Thông tin phiếu nghỉ phép: Xem nội dung đã mô tả  **Button Duyệt/ Từ chối chỉ hiển thị ở Tab Chờ duyệt &** **các phiếu có ngày tạo -> ngày hiện tại cách nhau 30 ngày**       * **Duyệt:**    + Khi người dùng nhấn vào button này, hệ thống hiển thị xác nhận: Bạn có muốn duyệt yêu cầu nghỉ phép này không?      - Chắc chắn: Hệ thống sẽ tự động thay đổi trạng thái của yêu cầu nghỉ phép đang được chọn từ "Chờ duyệt" sang "Đã duyệt" → Thông báo: Ghi nhận thành công! → Lưu thông tin Ngày cập nhật, Người cập nhật     - Xem lại: Tắt popup và quay về màn hình hiện tại  * **Từ chối:**    + Khi người dùng nhấn vào button này, hệ thống hiển thị xác nhận: Bạn có muốn từ chối duyệt yêu cầu nghỉ phép đã chọn?      - Chắc chắn:       * Hiển thị popup nhập lý do từ chối: Freetext (300)          + Xác nhận: Hệ thống sẽ tự động thay đổi trạng thái yêu cầu nghỉ phép đang được chọn từ "Chờ duyệt" sang "Từ chối" → Thông báo: Ghi nhận thành công! → Lưu thông tin Ngày cập nhật, Người cập nhật, Lý do từ chối duyệt         + Nhấn button X: Tắt popup và quay về màn hình hiện tại     - Trở lại: Tắt popup và quay về màn hình hiện tại   + Ghi nhận Từ chối/Duyệt thành công, hiển thị popup, auto đóng sau 2s:      * + Ghi nhận Từ chối/Duyệt thất bại, hiển thị popup kèm lỗi như sau: |
| **Tab Đã Duyệt** | * Thông tin yêu cầu nghỉ phép đã mô tả bên trên * Thời gian: Hiển thị thời gian chuyển trạng thái Đã duyệt |
| **Tab Từ chối** | * **Mặc định luôn hiển thị Lý do từ chối ở Tab này như hình:**      * Thông tin yêu cầu nghỉ phép đã mô tả bên trên * Thời gian: Hiển thị  thời gian chuyển trạng thái Từ chối * Nội dung lý do: Hiển thị thông tin lý do từ chối là giá trị đã nhập tại lý do từ chối duyệt |

# Màn hình liên quan

Báo cáo "Lịch sử chấm công" app QL: [Manager\_App] Lịch sử chấm công

* Rule màu của các thẻ:

* + Đỏ: Các case Không ghi nhận chấm công hoặc auto check out hoặc chỉ mới check in chưa check out
  + Xanh: Ghi nhận nghỉ phép được duyệt thành công. Có phiếu nghỉ phép nhưng vẫn chấm công thì hiển thị dữ liệu chấm công đầy đủ như các case khác.
  + Xám: chấm công đủ
* Trường hợp ngày hôm đó có thông tin nghỉ phép được duyệt thành công, khi nhấn chi tiết sẽ hiển thị chi tiết phiếu nghỉ phép, sẽ được mô tả ở chức năng Nghỉ phép
* Nghỉ phép chỉ tính phiếu nghỉ phép được duyệt thành công, các phiếu nghỉ phép chưa được duyệt hoặc Từ chối thì xem như chưa ghi nhận nghỉ phép.

* Trường hợp phiếu nghỉ phép đã duyệt → chuyển thành trạng thái "Chờ duyệt" => Thẻ chuyển màu từ Xanh sang Xám hoặc đỏ tương ứng và ẩn xem chi tiết phiếu nghỉ phép của các ngày nghỉ này.

Chi tiết phiếu nghỉ phép app QL: 

 